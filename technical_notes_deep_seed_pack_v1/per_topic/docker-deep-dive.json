{
  "mode": "upsert",
  "topics": [
    {
      "id": "docker-deep-dive",
      "slug": "docker-deep-dive",
      "title": "Docker: Images, Containers, Dockerfiles, Compose, Volumes, Networks, and Production Patterns",
      "subtitle": "Deep notes on Docker from mental model to production-ready containers, multi-stage builds, Compose, networking, volumes, health checks, and troubleshooting.",
      "summary": "Docker packages applications and their dependencies into portable images and runs them as isolated containers. These notes cover the practical engineering model needed for local development, CI/CD, and production deployment.",
      "group": "DevOps",
      "group_slug": "devops",
      "group_name": "DevOps",
      "domain": "Containers & Orchestration",
      "domain_slug": "containers-orchestration",
      "domain_name": "Containers & Orchestration",
      "status": "published",
      "content_format": "markdown",
      "difficulty": "advanced",
      "content": "# Docker: Images, Containers, Dockerfiles, Compose, Volumes, Networks, and Production Patterns\n\nDocker packages applications and their dependencies into portable images and runs them as isolated containers. These notes cover the practical engineering model needed for local development, CI/CD, and production deployment.\n\n## What Docker Solves\n\nDocker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: *it works on my machine but not on the server.*\n\n```mermaid\nflowchart LR\n    Code[Application Code] --> Dockerfile\n    Dockerfile --> Image[Immutable Image]\n    Image --> Registry[Docker Hub / ECR / GHCR]\n    Registry --> Container[Running Container]\n    Container --> Env[Dev / CI / Staging / Production]\n```\n\n**Mental model:**\n\n- **Image** = blueprint / artifact\n- **Container** = running instance of an image\n- **Dockerfile** = recipe for building image\n- **Registry** = storage/distribution for images\n- **Volume** = persistent data outside container lifecycle\n- **Network** = communication boundary between containers\n\n## Images, Layers, and Caching\n\nDocker images are built in layers. Each Dockerfile instruction generally creates a layer.\n\n```dockerfile\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nCMD [\"python\", \"main.py\"]\n```\n\nWhy copy `requirements.txt` before the rest of the source?\n\nBecause dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes.\n\n**Bad Dockerfile pattern:**\n\n```dockerfile\nCOPY . .\nRUN pip install -r requirements.txt\n```\n\nThis invalidates dependency cache whenever any file changes.\n\n## Containers and Isolation\n\nA container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation.\n\n| VM | Container |\n|---|---|\n| has full guest OS | shares host kernel |\n| heavier startup | faster startup |\n| strong isolation boundary | process-level isolation |\n| larger images | smaller images |\n\n**Production point:** containers are isolated, but not magically secure. You still need least privilege, non-root users, patched base images, secrets management, and network controls.\n\n## Dockerfile Best Practices\n\nGood Dockerfile practices:\n\n1. Use small base images when possible.\n2. Pin meaningful versions.\n3. Keep dependency layers before source-code layers.\n4. Use `.dockerignore`.\n5. Run as non-root.\n6. Prefer exec-form `CMD`.\n7. Avoid baking secrets into images.\n8. Use multi-stage builds.\n\nExample Node/React build:\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:1.27-alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n\n## Multi-Stage Builds\n\nMulti-stage builds let you use heavy build tools during compilation but ship only the final runtime artifact.\n\n```mermaid\nflowchart LR\n    Build[Build Stage: node/java/go/python deps] --> Artifact[Compiled / dist artifact]\n    Artifact --> Runtime[Runtime Stage: nginx/slim base]\n    Runtime --> Final[Small production image]\n```\n\nBenefits:\n\n- smaller final image\n- fewer vulnerabilities\n- cleaner runtime\n- faster deployment pulls\n\n## Volumes and Bind Mounts\n\nContainers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive.\n\n| Type | Use |\n|---|---|\n| Named volume | database data, persistent app storage |\n| Bind mount | local development source code |\n| tmpfs | temporary sensitive/runtime-only data |\n\nExample:\n\n```bash\ndocker volume create postgres-data\n\ndocker run -d \\\n  --name notes-db \\\n  -e POSTGRES_PASSWORD=secret \\\n  -v postgres-data:/var/lib/postgresql/data \\\n  postgres:16\n```\n\n**Common mistake:** running Postgres without a volume and then losing data when the container is recreated.\n\n## Docker Networks\n\nDocker containers can communicate through networks.\n\n```mermaid\nflowchart LR\n    Frontend[frontend container] -->|http://api:8000| API[api container]\n    API -->|postgres://db:5432| DB[db container]\n    subgraph bridge network\n        Frontend\n        API\n        DB\n    end\n```\n\nInside a Docker Compose network, service names become DNS names. If your service is named `api`, another container can call `http://api:8000`.\n\n**Important:** `localhost` inside a container means the container itself, not your host machine and not another container.\n\n## Docker Compose for Multi-Service Apps\n\nCompose defines multiple containers, their networks, volumes, ports, and environment.\n\n```yaml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      DATABASE_URL: postgresql://notes:notes@db:5432/notes\n    depends_on:\n      - db\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: notes\n      POSTGRES_PASSWORD: notes\n      POSTGRES_DB: notes\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```\n\n`depends_on` controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.\n\n## Dockerizing a FastAPI App\n\n```dockerfile\nFROM python:3.12-slim\n\nENV PYTHONDONTWRITEBYTECODE=1\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN pip install --no-cache-dir uv \\\n && uv sync --frozen --no-dev\n\nCOPY . .\n\nEXPOSE 8000\n\nCMD [\"uv\", \"run\", \"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nFor production, consider:\n\n- running behind a reverse proxy/load balancer\n- setting worker count intentionally\n- using health checks\n- ensuring logs go to stdout/stderr\n- using secrets through runtime environment, not image layers\n\n## Container Health Checks\n\nHealth checks tell orchestration systems whether a container should receive traffic or be restarted.\n\n```dockerfile\nHEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')\"\n```\n\nTypes of health:\n\n| Type | Meaning |\n|---|---|\n| live | process is alive |\n| ready | app is ready for traffic |\n| startup | app finished slow initialization |\n\nDo not make liveness depend on external systems unless you want external outage to restart every app container.\n\n## Security and Production Hygiene\n\nChecklist:\n\n- use non-root user\n- do not store `.env` or secrets in images\n- scan images\n- pin base images\n- remove build tools from runtime images\n- use read-only filesystem where possible\n- limit CPU/memory in production\n- log to stdout/stderr\n- use trusted registries\n- avoid `latest` in production\n\nExample non-root setup:\n\n```dockerfile\nRUN addgroup --system app && adduser --system --ingroup app app\nUSER app\n```\n\n## Docker Troubleshooting\n\nUseful commands:\n\n```bash\ndocker ps\ndocker ps -a\ndocker logs -f container_name\ndocker exec -it container_name sh\ndocker inspect container_name\ndocker network ls\ndocker volume ls\ndocker compose logs -f api\ndocker compose down -v\n```\n\nCommon issues:\n\n| Symptom | Likely cause |\n|---|---|\n| app cannot reach DB | wrong hostname; use Compose service name |\n| port already in use | host port conflict |\n| changes not reflected | image cache or no bind mount |\n| container exits instantly | bad CMD or missing env |\n| permission denied | non-root user cannot write mounted path |\n\n## Interview Questions and Answers\n\n**Q: Image vs container?**  \nA: Image is an immutable packaged artifact. Container is a running isolated process created from that image.\n\n**Q: Dockerfile vs Compose?**  \nA: Dockerfile builds an image. Compose defines how one or more containers run together.\n\n**Q: Why multi-stage builds?**  \nA: To keep build dependencies out of the final runtime image, reducing size and attack surface.\n\n**Q: Why is localhost confusing in Docker?**  \nA: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.\n\n",
      "body_markdown": "# Docker: Images, Containers, Dockerfiles, Compose, Volumes, Networks, and Production Patterns\n\nDocker packages applications and their dependencies into portable images and runs them as isolated containers. These notes cover the practical engineering model needed for local development, CI/CD, and production deployment.\n\n## What Docker Solves\n\nDocker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: *it works on my machine but not on the server.*\n\n```mermaid\nflowchart LR\n    Code[Application Code] --> Dockerfile\n    Dockerfile --> Image[Immutable Image]\n    Image --> Registry[Docker Hub / ECR / GHCR]\n    Registry --> Container[Running Container]\n    Container --> Env[Dev / CI / Staging / Production]\n```\n\n**Mental model:**\n\n- **Image** = blueprint / artifact\n- **Container** = running instance of an image\n- **Dockerfile** = recipe for building image\n- **Registry** = storage/distribution for images\n- **Volume** = persistent data outside container lifecycle\n- **Network** = communication boundary between containers\n\n## Images, Layers, and Caching\n\nDocker images are built in layers. Each Dockerfile instruction generally creates a layer.\n\n```dockerfile\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nCMD [\"python\", \"main.py\"]\n```\n\nWhy copy `requirements.txt` before the rest of the source?\n\nBecause dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes.\n\n**Bad Dockerfile pattern:**\n\n```dockerfile\nCOPY . .\nRUN pip install -r requirements.txt\n```\n\nThis invalidates dependency cache whenever any file changes.\n\n## Containers and Isolation\n\nA container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation.\n\n| VM | Container |\n|---|---|\n| has full guest OS | shares host kernel |\n| heavier startup | faster startup |\n| strong isolation boundary | process-level isolation |\n| larger images | smaller images |\n\n**Production point:** containers are isolated, but not magically secure. You still need least privilege, non-root users, patched base images, secrets management, and network controls.\n\n## Dockerfile Best Practices\n\nGood Dockerfile practices:\n\n1. Use small base images when possible.\n2. Pin meaningful versions.\n3. Keep dependency layers before source-code layers.\n4. Use `.dockerignore`.\n5. Run as non-root.\n6. Prefer exec-form `CMD`.\n7. Avoid baking secrets into images.\n8. Use multi-stage builds.\n\nExample Node/React build:\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:1.27-alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n\n## Multi-Stage Builds\n\nMulti-stage builds let you use heavy build tools during compilation but ship only the final runtime artifact.\n\n```mermaid\nflowchart LR\n    Build[Build Stage: node/java/go/python deps] --> Artifact[Compiled / dist artifact]\n    Artifact --> Runtime[Runtime Stage: nginx/slim base]\n    Runtime --> Final[Small production image]\n```\n\nBenefits:\n\n- smaller final image\n- fewer vulnerabilities\n- cleaner runtime\n- faster deployment pulls\n\n## Volumes and Bind Mounts\n\nContainers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive.\n\n| Type | Use |\n|---|---|\n| Named volume | database data, persistent app storage |\n| Bind mount | local development source code |\n| tmpfs | temporary sensitive/runtime-only data |\n\nExample:\n\n```bash\ndocker volume create postgres-data\n\ndocker run -d \\\n  --name notes-db \\\n  -e POSTGRES_PASSWORD=secret \\\n  -v postgres-data:/var/lib/postgresql/data \\\n  postgres:16\n```\n\n**Common mistake:** running Postgres without a volume and then losing data when the container is recreated.\n\n## Docker Networks\n\nDocker containers can communicate through networks.\n\n```mermaid\nflowchart LR\n    Frontend[frontend container] -->|http://api:8000| API[api container]\n    API -->|postgres://db:5432| DB[db container]\n    subgraph bridge network\n        Frontend\n        API\n        DB\n    end\n```\n\nInside a Docker Compose network, service names become DNS names. If your service is named `api`, another container can call `http://api:8000`.\n\n**Important:** `localhost` inside a container means the container itself, not your host machine and not another container.\n\n## Docker Compose for Multi-Service Apps\n\nCompose defines multiple containers, their networks, volumes, ports, and environment.\n\n```yaml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      DATABASE_URL: postgresql://notes:notes@db:5432/notes\n    depends_on:\n      - db\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: notes\n      POSTGRES_PASSWORD: notes\n      POSTGRES_DB: notes\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```\n\n`depends_on` controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.\n\n## Dockerizing a FastAPI App\n\n```dockerfile\nFROM python:3.12-slim\n\nENV PYTHONDONTWRITEBYTECODE=1\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN pip install --no-cache-dir uv \\\n && uv sync --frozen --no-dev\n\nCOPY . .\n\nEXPOSE 8000\n\nCMD [\"uv\", \"run\", \"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nFor production, consider:\n\n- running behind a reverse proxy/load balancer\n- setting worker count intentionally\n- using health checks\n- ensuring logs go to stdout/stderr\n- using secrets through runtime environment, not image layers\n\n## Container Health Checks\n\nHealth checks tell orchestration systems whether a container should receive traffic or be restarted.\n\n```dockerfile\nHEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')\"\n```\n\nTypes of health:\n\n| Type | Meaning |\n|---|---|\n| live | process is alive |\n| ready | app is ready for traffic |\n| startup | app finished slow initialization |\n\nDo not make liveness depend on external systems unless you want external outage to restart every app container.\n\n## Security and Production Hygiene\n\nChecklist:\n\n- use non-root user\n- do not store `.env` or secrets in images\n- scan images\n- pin base images\n- remove build tools from runtime images\n- use read-only filesystem where possible\n- limit CPU/memory in production\n- log to stdout/stderr\n- use trusted registries\n- avoid `latest` in production\n\nExample non-root setup:\n\n```dockerfile\nRUN addgroup --system app && adduser --system --ingroup app app\nUSER app\n```\n\n## Docker Troubleshooting\n\nUseful commands:\n\n```bash\ndocker ps\ndocker ps -a\ndocker logs -f container_name\ndocker exec -it container_name sh\ndocker inspect container_name\ndocker network ls\ndocker volume ls\ndocker compose logs -f api\ndocker compose down -v\n```\n\nCommon issues:\n\n| Symptom | Likely cause |\n|---|---|\n| app cannot reach DB | wrong hostname; use Compose service name |\n| port already in use | host port conflict |\n| changes not reflected | image cache or no bind mount |\n| container exits instantly | bad CMD or missing env |\n| permission denied | non-root user cannot write mounted path |\n\n## Interview Questions and Answers\n\n**Q: Image vs container?**  \nA: Image is an immutable packaged artifact. Container is a running isolated process created from that image.\n\n**Q: Dockerfile vs Compose?**  \nA: Dockerfile builds an image. Compose defines how one or more containers run together.\n\n**Q: Why multi-stage builds?**  \nA: To keep build dependencies out of the final runtime image, reducing size and attack surface.\n\n**Q: Why is localhost confusing in Docker?**  \nA: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.\n\n",
      "sections": [
        {
          "id": "docker-deep-dive__what-docker-solves",
          "slug": "what-docker-solves",
          "anchor": "docker-deep-dive__what-docker-solves",
          "title": "What Docker Solves",
          "level": 1,
          "order_index": 1,
          "parent_id": null,
          "content": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: *it works on my machine but not on the server.*\n\n```mermaid\nflowchart LR\n    Code[Application Code] --> Dockerfile\n    Dockerfile --> Image[Immutable Image]\n    Image --> Registry[Docker Hub / ECR / GHCR]\n    Registry --> Container[Running Container]\n    Container --> Env[Dev / CI / Staging / Production]\n```\n\n**Mental model:**\n\n- **Image** = blueprint / artifact\n- **Container** = running instance of an image\n- **Dockerfile** = recipe for building image\n- **Registry** = storage/distribution for images\n- **Volume** = persistent data outside container lifecycle\n- **Network** = communication boundary between containers\n",
          "body_markdown": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: *it works on my machine but not on the server.*\n\n```mermaid\nflowchart LR\n    Code[Application Code] --> Dockerfile\n    Dockerfile --> Image[Immutable Image]\n    Image --> Registry[Docker Hub / ECR / GHCR]\n    Registry --> Container[Running Container]\n    Container --> Env[Dev / CI / Staging / Production]\n```\n\n**Mental model:**\n\n- **Image** = blueprint / artifact\n- **Container** = running instance of an image\n- **Dockerfile** = recipe for building image\n- **Registry** = storage/distribution for images\n- **Volume** = persistent data outside container lifecycle\n- **Network** = communication boundary between containers\n",
          "rawText": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: it works on my machine but not on the server. code block Mental model: Image = blueprint / artifact Container = running instance of an image Dockerfile = recipe for building image Registry = storage/distribution for images Volume = persistent data outside container lifecycle Network = communication boundary between containers",
          "raw_text": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: it works on my machine but not on the server. code block Mental model: Image = blueprint / artifact Container = running instance of an image Dockerfile = recipe for building image Registry = storage/distribution for images Volume = persistent data outside container lifecycle Network = communication boundary between containers",
          "plainText": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: it works on my machine but not on the server. code block Mental model: Image = blueprint / artifact Container = running instance of an image Dockerfile = recipe for building image Registry = storage/distribution for images Volume = persistent data outside container lifecycle Network = communication boundary between containers",
          "plain_text": "Docker packages an application with its runtime dependencies into an image, then runs that image as a container. This solves the classic problem: it works on my machine but not on the server. code block Mental model: Image = blueprint / artifact Container = running instance of an image Dockerfile = recipe for building image Registry = storage/distribution for images Volume = persistent data outside container lifecycle Network = communication boundary between containers",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__images-layers-and-caching",
          "slug": "images-layers-and-caching",
          "anchor": "docker-deep-dive__images-layers-and-caching",
          "title": "Images, Layers, and Caching",
          "level": 1,
          "order_index": 2,
          "parent_id": null,
          "content": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer.\n\n```dockerfile\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nCMD [\"python\", \"main.py\"]\n```\n\nWhy copy `requirements.txt` before the rest of the source?\n\nBecause dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes.\n\n**Bad Dockerfile pattern:**\n\n```dockerfile\nCOPY . .\nRUN pip install -r requirements.txt\n```\n\nThis invalidates dependency cache whenever any file changes.\n",
          "body_markdown": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer.\n\n```dockerfile\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nCMD [\"python\", \"main.py\"]\n```\n\nWhy copy `requirements.txt` before the rest of the source?\n\nBecause dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes.\n\n**Bad Dockerfile pattern:**\n\n```dockerfile\nCOPY . .\nRUN pip install -r requirements.txt\n```\n\nThis invalidates dependency cache whenever any file changes.\n",
          "rawText": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer. code block Why copy requirements.txt before the rest of the source? Because dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes. Bad Dockerfile pattern: code block This invalidates dependency cache whenever any file changes.",
          "raw_text": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer. code block Why copy requirements.txt before the rest of the source? Because dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes. Bad Dockerfile pattern: code block This invalidates dependency cache whenever any file changes.",
          "plainText": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer. code block Why copy requirements.txt before the rest of the source? Because dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes. Bad Dockerfile pattern: code block This invalidates dependency cache whenever any file changes.",
          "plain_text": "Docker images are built in layers. Each Dockerfile instruction generally creates a layer. code block Why copy requirements.txt before the rest of the source? Because dependency installation changes less frequently than application code. This lets Docker reuse the expensive dependency layer when only code changes. Bad Dockerfile pattern: code block This invalidates dependency cache whenever any file changes.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__containers-and-isolation",
          "slug": "containers-and-isolation",
          "anchor": "docker-deep-dive__containers-and-isolation",
          "title": "Containers and Isolation",
          "level": 1,
          "order_index": 3,
          "parent_id": null,
          "content": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation.\n\n| VM | Container |\n|---|---|\n| has full guest OS | shares host kernel |\n| heavier startup | faster startup |\n| strong isolation boundary | process-level isolation |\n| larger images | smaller images |\n\n**Production point:** containers are isolated, but not magically secure. You still need least privilege, non-root users, patched base images, secrets management, and network controls.\n",
          "body_markdown": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation.\n\n| VM | Container |\n|---|---|\n| has full guest OS | shares host kernel |\n| heavier startup | faster startup |\n| strong isolation boundary | process-level isolation |\n| larger images | smaller images |\n\n**Production point:** containers are isolated, but not magically secure. You still need least privilege, non-root users, patched base images, secrets management, and network controls.\n",
          "rawText": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation. VM Container has full guest OS shares host kernel heavier startup faster startup strong isolation boundary process level isolation larger images smaller images Production point: containers are isolated, but not magically secure. You still need least privilege, non root users, patched base images, secrets management, and network controls.",
          "raw_text": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation. VM Container has full guest OS shares host kernel heavier startup faster startup strong isolation boundary process level isolation larger images smaller images Production point: containers are isolated, but not magically secure. You still need least privilege, non root users, patched base images, secrets management, and network controls.",
          "plainText": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation. VM Container has full guest OS shares host kernel heavier startup faster startup strong isolation boundary process level isolation larger images smaller images Production point: containers are isolated, but not magically secure. You still need least privilege, non root users, patched base images, secrets management, and network controls.",
          "plain_text": "A container is not a full virtual machine. It is a process isolated using OS features such as namespaces, cgroups, filesystem layers, and network isolation. VM Container has full guest OS shares host kernel heavier startup faster startup strong isolation boundary process level isolation larger images smaller images Production point: containers are isolated, but not magically secure. You still need least privilege, non root users, patched base images, secrets management, and network controls.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__dockerfile-best-practices",
          "slug": "dockerfile-best-practices",
          "anchor": "docker-deep-dive__dockerfile-best-practices",
          "title": "Dockerfile Best Practices",
          "level": 1,
          "order_index": 4,
          "parent_id": null,
          "content": "Good Dockerfile practices:\n\n1. Use small base images when possible.\n2. Pin meaningful versions.\n3. Keep dependency layers before source-code layers.\n4. Use `.dockerignore`.\n5. Run as non-root.\n6. Prefer exec-form `CMD`.\n7. Avoid baking secrets into images.\n8. Use multi-stage builds.\n\nExample Node/React build:\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:1.27-alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n",
          "body_markdown": "Good Dockerfile practices:\n\n1. Use small base images when possible.\n2. Pin meaningful versions.\n3. Keep dependency layers before source-code layers.\n4. Use `.dockerignore`.\n5. Run as non-root.\n6. Prefer exec-form `CMD`.\n7. Avoid baking secrets into images.\n8. Use multi-stage builds.\n\nExample Node/React build:\n\n```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:1.27-alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\nCMD [\"nginx\", \"-g\", \"daemon off;\"]\n```\n",
          "rawText": "Good Dockerfile practices: 1. Use small base images when possible. 2. Pin meaningful versions. 3. Keep dependency layers before source code layers. 4. Use .dockerignore. 5. Run as non root. 6. Prefer exec form CMD. 7. Avoid baking secrets into images. 8. Use multi stage builds. Example Node/React build: code block",
          "raw_text": "Good Dockerfile practices: 1. Use small base images when possible. 2. Pin meaningful versions. 3. Keep dependency layers before source code layers. 4. Use .dockerignore. 5. Run as non root. 6. Prefer exec form CMD. 7. Avoid baking secrets into images. 8. Use multi stage builds. Example Node/React build: code block",
          "plainText": "Good Dockerfile practices: 1. Use small base images when possible. 2. Pin meaningful versions. 3. Keep dependency layers before source code layers. 4. Use .dockerignore. 5. Run as non root. 6. Prefer exec form CMD. 7. Avoid baking secrets into images. 8. Use multi stage builds. Example Node/React build: code block",
          "plain_text": "Good Dockerfile practices: 1. Use small base images when possible. 2. Pin meaningful versions. 3. Keep dependency layers before source code layers. 4. Use .dockerignore. 5. Run as non root. 6. Prefer exec form CMD. 7. Avoid baking secrets into images. 8. Use multi stage builds. Example Node/React build: code block",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__multi-stage-builds",
          "slug": "multi-stage-builds",
          "anchor": "docker-deep-dive__multi-stage-builds",
          "title": "Multi-Stage Builds",
          "level": 1,
          "order_index": 5,
          "parent_id": null,
          "content": "Multi-stage builds let you use heavy build tools during compilation but ship only the final runtime artifact.\n\n```mermaid\nflowchart LR\n    Build[Build Stage: node/java/go/python deps] --> Artifact[Compiled / dist artifact]\n    Artifact --> Runtime[Runtime Stage: nginx/slim base]\n    Runtime --> Final[Small production image]\n```\n\nBenefits:\n\n- smaller final image\n- fewer vulnerabilities\n- cleaner runtime\n- faster deployment pulls\n",
          "body_markdown": "Multi-stage builds let you use heavy build tools during compilation but ship only the final runtime artifact.\n\n```mermaid\nflowchart LR\n    Build[Build Stage: node/java/go/python deps] --> Artifact[Compiled / dist artifact]\n    Artifact --> Runtime[Runtime Stage: nginx/slim base]\n    Runtime --> Final[Small production image]\n```\n\nBenefits:\n\n- smaller final image\n- fewer vulnerabilities\n- cleaner runtime\n- faster deployment pulls\n",
          "rawText": "Multi stage builds let you use heavy build tools during compilation but ship only the final runtime artifact. code block Benefits: smaller final image fewer vulnerabilities cleaner runtime faster deployment pulls",
          "raw_text": "Multi stage builds let you use heavy build tools during compilation but ship only the final runtime artifact. code block Benefits: smaller final image fewer vulnerabilities cleaner runtime faster deployment pulls",
          "plainText": "Multi stage builds let you use heavy build tools during compilation but ship only the final runtime artifact. code block Benefits: smaller final image fewer vulnerabilities cleaner runtime faster deployment pulls",
          "plain_text": "Multi stage builds let you use heavy build tools during compilation but ship only the final runtime artifact. code block Benefits: smaller final image fewer vulnerabilities cleaner runtime faster deployment pulls",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__volumes-and-bind-mounts",
          "slug": "volumes-and-bind-mounts",
          "anchor": "docker-deep-dive__volumes-and-bind-mounts",
          "title": "Volumes and Bind Mounts",
          "level": 1,
          "order_index": 6,
          "parent_id": null,
          "content": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive.\n\n| Type | Use |\n|---|---|\n| Named volume | database data, persistent app storage |\n| Bind mount | local development source code |\n| tmpfs | temporary sensitive/runtime-only data |\n\nExample:\n\n```bash\ndocker volume create postgres-data\n\ndocker run -d \\\n  --name notes-db \\\n  -e POSTGRES_PASSWORD=secret \\\n  -v postgres-data:/var/lib/postgresql/data \\\n  postgres:16\n```\n\n**Common mistake:** running Postgres without a volume and then losing data when the container is recreated.\n",
          "body_markdown": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive.\n\n| Type | Use |\n|---|---|\n| Named volume | database data, persistent app storage |\n| Bind mount | local development source code |\n| tmpfs | temporary sensitive/runtime-only data |\n\nExample:\n\n```bash\ndocker volume create postgres-data\n\ndocker run -d \\\n  --name notes-db \\\n  -e POSTGRES_PASSWORD=secret \\\n  -v postgres-data:/var/lib/postgresql/data \\\n  postgres:16\n```\n\n**Common mistake:** running Postgres without a volume and then losing data when the container is recreated.\n",
          "rawText": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive. Type Use Named volume database data, persistent app storage Bind mount local development source code tmpfs temporary sensitive/runtime only data Example: code block Common mistake: running Postgres without a volume and then losing data when the container is recreated.",
          "raw_text": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive. Type Use Named volume database data, persistent app storage Bind mount local development source code tmpfs temporary sensitive/runtime only data Example: code block Common mistake: running Postgres without a volume and then losing data when the container is recreated.",
          "plainText": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive. Type Use Named volume database data, persistent app storage Bind mount local development source code tmpfs temporary sensitive/runtime only data Example: code block Common mistake: running Postgres without a volume and then losing data when the container is recreated.",
          "plain_text": "Containers are disposable. If a container is removed, its writable layer is removed too. Use storage mounts when data must survive. Type Use Named volume database data, persistent app storage Bind mount local development source code tmpfs temporary sensitive/runtime only data Example: code block Common mistake: running Postgres without a volume and then losing data when the container is recreated.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__docker-networks",
          "slug": "docker-networks",
          "anchor": "docker-deep-dive__docker-networks",
          "title": "Docker Networks",
          "level": 1,
          "order_index": 7,
          "parent_id": null,
          "content": "Docker containers can communicate through networks.\n\n```mermaid\nflowchart LR\n    Frontend[frontend container] -->|http://api:8000| API[api container]\n    API -->|postgres://db:5432| DB[db container]\n    subgraph bridge network\n        Frontend\n        API\n        DB\n    end\n```\n\nInside a Docker Compose network, service names become DNS names. If your service is named `api`, another container can call `http://api:8000`.\n\n**Important:** `localhost` inside a container means the container itself, not your host machine and not another container.\n",
          "body_markdown": "Docker containers can communicate through networks.\n\n```mermaid\nflowchart LR\n    Frontend[frontend container] -->|http://api:8000| API[api container]\n    API -->|postgres://db:5432| DB[db container]\n    subgraph bridge network\n        Frontend\n        API\n        DB\n    end\n```\n\nInside a Docker Compose network, service names become DNS names. If your service is named `api`, another container can call `http://api:8000`.\n\n**Important:** `localhost` inside a container means the container itself, not your host machine and not another container.\n",
          "rawText": "Docker containers can communicate through networks. code block Inside a Docker Compose network, service names become DNS names. If your service is named api, another container can call http://api:8000. Important: localhost inside a container means the container itself, not your host machine and not another container.",
          "raw_text": "Docker containers can communicate through networks. code block Inside a Docker Compose network, service names become DNS names. If your service is named api, another container can call http://api:8000. Important: localhost inside a container means the container itself, not your host machine and not another container.",
          "plainText": "Docker containers can communicate through networks. code block Inside a Docker Compose network, service names become DNS names. If your service is named api, another container can call http://api:8000. Important: localhost inside a container means the container itself, not your host machine and not another container.",
          "plain_text": "Docker containers can communicate through networks. code block Inside a Docker Compose network, service names become DNS names. If your service is named api, another container can call http://api:8000. Important: localhost inside a container means the container itself, not your host machine and not another container.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__docker-compose-for-multi-service-apps",
          "slug": "docker-compose-for-multi-service-apps",
          "anchor": "docker-deep-dive__docker-compose-for-multi-service-apps",
          "title": "Docker Compose for Multi-Service Apps",
          "level": 1,
          "order_index": 8,
          "parent_id": null,
          "content": "Compose defines multiple containers, their networks, volumes, ports, and environment.\n\n```yaml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      DATABASE_URL: postgresql://notes:notes@db:5432/notes\n    depends_on:\n      - db\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: notes\n      POSTGRES_PASSWORD: notes\n      POSTGRES_DB: notes\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```\n\n`depends_on` controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.\n",
          "body_markdown": "Compose defines multiple containers, their networks, volumes, ports, and environment.\n\n```yaml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      DATABASE_URL: postgresql://notes:notes@db:5432/notes\n    depends_on:\n      - db\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: notes\n      POSTGRES_PASSWORD: notes\n      POSTGRES_DB: notes\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n\nvolumes:\n  pgdata:\n```\n\n`depends_on` controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.\n",
          "rawText": "Compose defines multiple containers, their networks, volumes, ports, and environment. code block depends on controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.",
          "raw_text": "Compose defines multiple containers, their networks, volumes, ports, and environment. code block depends on controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.",
          "plainText": "Compose defines multiple containers, their networks, volumes, ports, and environment. code block depends on controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.",
          "plain_text": "Compose defines multiple containers, their networks, volumes, ports, and environment. code block depends on controls startup ordering, not true readiness. Your app should retry DB connections or use health checks.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__dockerizing-a-fastapi-app",
          "slug": "dockerizing-a-fastapi-app",
          "anchor": "docker-deep-dive__dockerizing-a-fastapi-app",
          "title": "Dockerizing a FastAPI App",
          "level": 1,
          "order_index": 9,
          "parent_id": null,
          "content": "```dockerfile\nFROM python:3.12-slim\n\nENV PYTHONDONTWRITEBYTECODE=1\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN pip install --no-cache-dir uv \\\n && uv sync --frozen --no-dev\n\nCOPY . .\n\nEXPOSE 8000\n\nCMD [\"uv\", \"run\", \"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nFor production, consider:\n\n- running behind a reverse proxy/load balancer\n- setting worker count intentionally\n- using health checks\n- ensuring logs go to stdout/stderr\n- using secrets through runtime environment, not image layers\n",
          "body_markdown": "```dockerfile\nFROM python:3.12-slim\n\nENV PYTHONDONTWRITEBYTECODE=1\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN pip install --no-cache-dir uv \\\n && uv sync --frozen --no-dev\n\nCOPY . .\n\nEXPOSE 8000\n\nCMD [\"uv\", \"run\", \"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nFor production, consider:\n\n- running behind a reverse proxy/load balancer\n- setting worker count intentionally\n- using health checks\n- ensuring logs go to stdout/stderr\n- using secrets through runtime environment, not image layers\n",
          "rawText": "code block For production, consider: running behind a reverse proxy/load balancer setting worker count intentionally using health checks ensuring logs go to stdout/stderr using secrets through runtime environment, not image layers",
          "raw_text": "code block For production, consider: running behind a reverse proxy/load balancer setting worker count intentionally using health checks ensuring logs go to stdout/stderr using secrets through runtime environment, not image layers",
          "plainText": "code block For production, consider: running behind a reverse proxy/load balancer setting worker count intentionally using health checks ensuring logs go to stdout/stderr using secrets through runtime environment, not image layers",
          "plain_text": "code block For production, consider: running behind a reverse proxy/load balancer setting worker count intentionally using health checks ensuring logs go to stdout/stderr using secrets through runtime environment, not image layers",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__container-health-checks",
          "slug": "container-health-checks",
          "anchor": "docker-deep-dive__container-health-checks",
          "title": "Container Health Checks",
          "level": 1,
          "order_index": 10,
          "parent_id": null,
          "content": "Health checks tell orchestration systems whether a container should receive traffic or be restarted.\n\n```dockerfile\nHEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')\"\n```\n\nTypes of health:\n\n| Type | Meaning |\n|---|---|\n| live | process is alive |\n| ready | app is ready for traffic |\n| startup | app finished slow initialization |\n\nDo not make liveness depend on external systems unless you want external outage to restart every app container.\n",
          "body_markdown": "Health checks tell orchestration systems whether a container should receive traffic or be restarted.\n\n```dockerfile\nHEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')\"\n```\n\nTypes of health:\n\n| Type | Meaning |\n|---|---|\n| live | process is alive |\n| ready | app is ready for traffic |\n| startup | app finished slow initialization |\n\nDo not make liveness depend on external systems unless you want external outage to restart every app container.\n",
          "rawText": "Health checks tell orchestration systems whether a container should receive traffic or be restarted. code block Types of health: Type Meaning live process is alive ready app is ready for traffic startup app finished slow initialization Do not make liveness depend on external systems unless you want external outage to restart every app container.",
          "raw_text": "Health checks tell orchestration systems whether a container should receive traffic or be restarted. code block Types of health: Type Meaning live process is alive ready app is ready for traffic startup app finished slow initialization Do not make liveness depend on external systems unless you want external outage to restart every app container.",
          "plainText": "Health checks tell orchestration systems whether a container should receive traffic or be restarted. code block Types of health: Type Meaning live process is alive ready app is ready for traffic startup app finished slow initialization Do not make liveness depend on external systems unless you want external outage to restart every app container.",
          "plain_text": "Health checks tell orchestration systems whether a container should receive traffic or be restarted. code block Types of health: Type Meaning live process is alive ready app is ready for traffic startup app finished slow initialization Do not make liveness depend on external systems unless you want external outage to restart every app container.",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__security-and-production-hygiene",
          "slug": "security-and-production-hygiene",
          "anchor": "docker-deep-dive__security-and-production-hygiene",
          "title": "Security and Production Hygiene",
          "level": 1,
          "order_index": 11,
          "parent_id": null,
          "content": "Checklist:\n\n- use non-root user\n- do not store `.env` or secrets in images\n- scan images\n- pin base images\n- remove build tools from runtime images\n- use read-only filesystem where possible\n- limit CPU/memory in production\n- log to stdout/stderr\n- use trusted registries\n- avoid `latest` in production\n\nExample non-root setup:\n\n```dockerfile\nRUN addgroup --system app && adduser --system --ingroup app app\nUSER app\n```\n",
          "body_markdown": "Checklist:\n\n- use non-root user\n- do not store `.env` or secrets in images\n- scan images\n- pin base images\n- remove build tools from runtime images\n- use read-only filesystem where possible\n- limit CPU/memory in production\n- log to stdout/stderr\n- use trusted registries\n- avoid `latest` in production\n\nExample non-root setup:\n\n```dockerfile\nRUN addgroup --system app && adduser --system --ingroup app app\nUSER app\n```\n",
          "rawText": "Checklist: use non root user do not store .env or secrets in images scan images pin base images remove build tools from runtime images use read only filesystem where possible limit CPU/memory in production log to stdout/stderr use trusted registries avoid latest in production Example non root setup: code block",
          "raw_text": "Checklist: use non root user do not store .env or secrets in images scan images pin base images remove build tools from runtime images use read only filesystem where possible limit CPU/memory in production log to stdout/stderr use trusted registries avoid latest in production Example non root setup: code block",
          "plainText": "Checklist: use non root user do not store .env or secrets in images scan images pin base images remove build tools from runtime images use read only filesystem where possible limit CPU/memory in production log to stdout/stderr use trusted registries avoid latest in production Example non root setup: code block",
          "plain_text": "Checklist: use non root user do not store .env or secrets in images scan images pin base images remove build tools from runtime images use read only filesystem where possible limit CPU/memory in production log to stdout/stderr use trusted registries avoid latest in production Example non root setup: code block",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__docker-troubleshooting",
          "slug": "docker-troubleshooting",
          "anchor": "docker-deep-dive__docker-troubleshooting",
          "title": "Docker Troubleshooting",
          "level": 1,
          "order_index": 12,
          "parent_id": null,
          "content": "Useful commands:\n\n```bash\ndocker ps\ndocker ps -a\ndocker logs -f container_name\ndocker exec -it container_name sh\ndocker inspect container_name\ndocker network ls\ndocker volume ls\ndocker compose logs -f api\ndocker compose down -v\n```\n\nCommon issues:\n\n| Symptom | Likely cause |\n|---|---|\n| app cannot reach DB | wrong hostname; use Compose service name |\n| port already in use | host port conflict |\n| changes not reflected | image cache or no bind mount |\n| container exits instantly | bad CMD or missing env |\n| permission denied | non-root user cannot write mounted path |\n",
          "body_markdown": "Useful commands:\n\n```bash\ndocker ps\ndocker ps -a\ndocker logs -f container_name\ndocker exec -it container_name sh\ndocker inspect container_name\ndocker network ls\ndocker volume ls\ndocker compose logs -f api\ndocker compose down -v\n```\n\nCommon issues:\n\n| Symptom | Likely cause |\n|---|---|\n| app cannot reach DB | wrong hostname; use Compose service name |\n| port already in use | host port conflict |\n| changes not reflected | image cache or no bind mount |\n| container exits instantly | bad CMD or missing env |\n| permission denied | non-root user cannot write mounted path |\n",
          "rawText": "Useful commands: code block Common issues: Symptom Likely cause app cannot reach DB wrong hostname; use Compose service name port already in use host port conflict changes not reflected image cache or no bind mount container exits instantly bad CMD or missing env permission denied non root user cannot write mounted path",
          "raw_text": "Useful commands: code block Common issues: Symptom Likely cause app cannot reach DB wrong hostname; use Compose service name port already in use host port conflict changes not reflected image cache or no bind mount container exits instantly bad CMD or missing env permission denied non root user cannot write mounted path",
          "plainText": "Useful commands: code block Common issues: Symptom Likely cause app cannot reach DB wrong hostname; use Compose service name port already in use host port conflict changes not reflected image cache or no bind mount container exits instantly bad CMD or missing env permission denied non root user cannot write mounted path",
          "plain_text": "Useful commands: code block Common issues: Symptom Likely cause app cannot reach DB wrong hostname; use Compose service name port already in use host port conflict changes not reflected image cache or no bind mount container exits instantly bad CMD or missing env permission denied non root user cannot write mounted path",
          "kind": "section"
        },
        {
          "id": "docker-deep-dive__interview-questions-and-answers",
          "slug": "interview-questions-and-answers",
          "anchor": "docker-deep-dive__interview-questions-and-answers",
          "title": "Interview Questions and Answers",
          "level": 1,
          "order_index": 13,
          "parent_id": null,
          "content": "**Q: Image vs container?**  \nA: Image is an immutable packaged artifact. Container is a running isolated process created from that image.\n\n**Q: Dockerfile vs Compose?**  \nA: Dockerfile builds an image. Compose defines how one or more containers run together.\n\n**Q: Why multi-stage builds?**  \nA: To keep build dependencies out of the final runtime image, reducing size and attack surface.\n\n**Q: Why is localhost confusing in Docker?**  \nA: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.\n",
          "body_markdown": "**Q: Image vs container?**  \nA: Image is an immutable packaged artifact. Container is a running isolated process created from that image.\n\n**Q: Dockerfile vs Compose?**  \nA: Dockerfile builds an image. Compose defines how one or more containers run together.\n\n**Q: Why multi-stage builds?**  \nA: To keep build dependencies out of the final runtime image, reducing size and attack surface.\n\n**Q: Why is localhost confusing in Docker?**  \nA: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.\n",
          "rawText": "Q: Image vs container? A: Image is an immutable packaged artifact. Container is a running isolated process created from that image. Q: Dockerfile vs Compose? A: Dockerfile builds an image. Compose defines how one or more containers run together. Q: Why multi stage builds? A: To keep build dependencies out of the final runtime image, reducing size and attack surface. Q: Why is localhost confusing in Docker? A: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.",
          "raw_text": "Q: Image vs container? A: Image is an immutable packaged artifact. Container is a running isolated process created from that image. Q: Dockerfile vs Compose? A: Dockerfile builds an image. Compose defines how one or more containers run together. Q: Why multi stage builds? A: To keep build dependencies out of the final runtime image, reducing size and attack surface. Q: Why is localhost confusing in Docker? A: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.",
          "plainText": "Q: Image vs container? A: Image is an immutable packaged artifact. Container is a running isolated process created from that image. Q: Dockerfile vs Compose? A: Dockerfile builds an image. Compose defines how one or more containers run together. Q: Why multi stage builds? A: To keep build dependencies out of the final runtime image, reducing size and attack surface. Q: Why is localhost confusing in Docker? A: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.",
          "plain_text": "Q: Image vs container? A: Image is an immutable packaged artifact. Container is a running isolated process created from that image. Q: Dockerfile vs Compose? A: Dockerfile builds an image. Compose defines how one or more containers run together. Q: Why multi stage builds? A: To keep build dependencies out of the final runtime image, reducing size and attack surface. Q: Why is localhost confusing in Docker? A: Inside a container, localhost refers to that container. Other services must be reached by container/service name or network address.",
          "kind": "section"
        }
      ],
      "tags": [
        "docker",
        "containers",
        "devops",
        "compose",
        "dockerfile"
      ],
      "sourceFiles": [
        "Docker Docs - Docker Overview",
        "Docker Docs - Docker Compose",
        "Docker Docs - Persisting Container Data",
        "Docker Docs - Dockerfile vs Compose File"
      ],
      "source_keys": [
        "docker-docs-docker-overview",
        "docker-docs-docker-compose",
        "docker-docs-persisting-container-data",
        "docker-docs-dockerfile-vs-compose-file"
      ],
      "order_index": 820,
      "is_featured": true,
      "metadata": {
        "seed_version": "deep-additions-v1",
        "generated_at": "2026-06-27",
        "intended_endpoint": "/api/v1/admin/topics/bulk-upsert-fast?rebuild_bootstrap=true",
        "source_refs": [
          {
            "label": "Docker Docs - Docker Overview",
            "url": "https://docs.docker.com/get-started/docker-overview/"
          },
          {
            "label": "Docker Docs - Docker Compose",
            "url": "https://docs.docker.com/compose/"
          },
          {
            "label": "Docker Docs - Persisting Container Data",
            "url": "https://docs.docker.com/get-started/docker-concepts/running-containers/persisting-container-data/"
          },
          {
            "label": "Docker Docs - Dockerfile vs Compose File",
            "url": "https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-docker-compose/"
          }
        ],
        "section_strategy": "flat sections with level + parent_id + stable anchors"
      },
      "extra": {
        "learning_outcomes": [
          "Explain the concept in interviews with production-grade clarity",
          "Recognize common mistakes and debug real issues",
          "Apply the topic with working code/configuration examples",
          "Use diagrams to build a mental model before memorizing syntax"
        ],
        "recommended_rendering": {
          "left_sidebar": "topic tree",
          "right_toc": "sections by anchor",
          "scrollspy_key": "section.anchor"
        }
      }
    }
  ]
}