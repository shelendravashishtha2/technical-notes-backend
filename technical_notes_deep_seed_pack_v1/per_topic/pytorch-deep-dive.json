{
  "mode": "upsert",
  "topics": [
    {
      "id": "pytorch-deep-dive",
      "slug": "pytorch-deep-dive",
      "title": "PyTorch: Tensors, Autograd, nn.Module, DataLoaders, Training Loops, and Production ML Patterns",
      "subtitle": "Deep notes on PyTorch fundamentals and practical training: tensors, devices, autograd, modules, DataLoader, losses, optimizers, evaluation, saving/loading, and common mistakes.",
      "summary": "PyTorch provides tensor computation, automatic differentiation, neural network abstractions, and GPU acceleration. These notes explain how to build, train, debug, and reason about models in a practical engineering style.",
      "group": "Machine Learning",
      "group_slug": "machine-learning",
      "group_name": "Machine Learning",
      "domain": "AI & Data Science",
      "domain_slug": "ai-data-science",
      "domain_name": "AI & Data Science",
      "status": "published",
      "content_format": "markdown",
      "difficulty": "advanced",
      "content": "# PyTorch: Tensors, Autograd, nn.Module, DataLoaders, Training Loops, and Production ML Patterns\n\nPyTorch provides tensor computation, automatic differentiation, neural network abstractions, and GPU acceleration. These notes explain how to build, train, debug, and reason about models in a practical engineering style.\n\n## What PyTorch Solves\n\nPyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration.\n\nAt a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning.\n\n```mermaid\nflowchart LR\n    Data[Dataset] --> Loader[DataLoader]\n    Loader --> Batch[Mini-batches]\n    Batch --> Model[nn.Module]\n    Model --> Pred[Predictions]\n    Pred --> Loss[Loss Function]\n    Loss --> Backward[loss.backward()]\n    Backward --> Optim[optimizer.step()]\n    Optim --> Model\n```\n\n**Mental model:** PyTorch training is repeated numeric transformation plus gradient-based parameter updates.\n\n## Tensors\n\nA tensor is a multi-dimensional array used to store inputs, outputs, labels, and model parameters.\n\n```python\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]])\nw = torch.randn(2, 3)\n\nprint(x.shape)   # torch.Size([2, 2])\nprint(w.dtype)   # torch.float32 by default\nprint(w.device)  # cpu or cuda/mps/xpu\n```\n\nCommon tensor dimensions:\n\n| Shape | Meaning |\n|---|---|\n| `()` | scalar |\n| `(features,)` | single vector |\n| `(batch, features)` | tabular batch |\n| `(batch, channels, height, width)` | image batch |\n| `(batch, sequence, embedding)` | NLP sequence batch |\n\n**Common mistake:** shape mismatch. Always print shapes at model boundaries.\n\n## Devices: CPU, CUDA, MPS\n\nModels and tensors must be on the same device.\n\n```python\nimport torch\n\ndevice = (\n    \"cuda\" if torch.cuda.is_available()\n    else \"mps\" if torch.backends.mps.is_available()\n    else \"cpu\"\n)\n\nmodel = model.to(device)\nx = x.to(device)\ny = y.to(device)\n```\n\nIf model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error.\n\n**Interview explanation:** GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.\n\n## Autograd and Computation Graphs\n\nAutograd tracks operations on tensors with `requires_grad=True` and computes gradients during backpropagation.\n\n```python\nimport torch\n\nx = torch.tensor([2.0], requires_grad=True)\ny = x ** 2 + 3 * x + 1\ny.backward()\n\nprint(x.grad)  # dy/dx = 2x + 3 = 7\n```\n\n```mermaid\nflowchart LR\n    X[x requires_grad] --> POW[x^2]\n    X --> MUL[3x]\n    POW --> SUM[y]\n    MUL --> SUM\n    SUM --> BACK[backward]\n    BACK --> GRAD[x.grad]\n```\n\nAutograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.\n\n## nn.Module\n\n`nn.Module` is the base class for neural networks. A model usually defines layers in `__init__` and computation in `forward`.\n\n```python\nfrom torch import nn\n\nclass MLP(nn.Module):\n    def __init__(self, in_features: int, num_classes: int):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(in_features, 128),\n            nn.ReLU(),\n            nn.Dropout(0.2),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n```\n\nWhy subclass `nn.Module`?\n\n- parameters are registered automatically\n- `.to(device)` moves all parameters\n- `.train()` and `.eval()` switch behavior of dropout/batchnorm\n- `state_dict()` enables saving/loading\n\n## Datasets and DataLoaders\n\nA `Dataset` knows how to access one sample. A `DataLoader` batches, shuffles, and parallelizes loading.\n\n```python\nfrom torch.utils.data import Dataset, DataLoader\n\nclass NotesDataset(Dataset):\n    def __init__(self, rows, tokenizer):\n        self.rows = rows\n        self.tokenizer = tokenizer\n\n    def __len__(self):\n        return len(self.rows)\n\n    def __getitem__(self, idx):\n        text = self.rows[idx][\"text\"]\n        label = self.rows[idx][\"label\"]\n        encoded = self.tokenizer(text)\n        return encoded, label\n\nloader = DataLoader(\n    NotesDataset(rows, tokenizer),\n    batch_size=32,\n    shuffle=True,\n    num_workers=4,\n)\n```\n\n**Good separation:** dataset handles sample access; model handles prediction; training loop handles optimization.\n\n## Training Loop\n\nA basic supervised training loop:\n\n```python\nimport torch\nfrom torch import nn\n\ndef train_one_epoch(model, loader, optimizer, loss_fn, device):\n    model.train()\n    total_loss = 0.0\n\n    for x, y in loader:\n        x = x.to(device)\n        y = y.to(device)\n\n        optimizer.zero_grad(set_to_none=True)\n        logits = model(x)\n        loss = loss_fn(logits, y)\n        loss.backward()\n        optimizer.step()\n\n        total_loss += loss.item()\n\n    return total_loss / len(loader)\n```\n\nOrder matters:\n\n```txt\nzero gradients -> forward -> compute loss -> backward -> optimizer step\n```\n\nIf you forget `optimizer.zero_grad()`, gradients accumulate across batches.\n\n## Evaluation Loop\n\nEvaluation should disable gradient tracking and switch the model to eval mode.\n\n```python\ndef evaluate(model, loader, loss_fn, device):\n    model.eval()\n    correct = 0\n    total = 0\n    total_loss = 0.0\n\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(device), y.to(device)\n            logits = model(x)\n            loss = loss_fn(logits, y)\n            total_loss += loss.item()\n\n            preds = logits.argmax(dim=1)\n            correct += (preds == y).sum().item()\n            total += y.numel()\n\n    return {\n        \"loss\": total_loss / len(loader),\n        \"accuracy\": correct / total,\n    }\n```\n\n`torch.no_grad()` reduces memory use because PyTorch does not store intermediate tensors for gradients.\n\n## Loss Functions and Optimizers\n\nCommon pairings:\n\n| Task | Output | Loss |\n|---|---|---|\n| multi-class classification | raw logits `(N, C)` | `nn.CrossEntropyLoss()` |\n| binary classification | logits `(N, 1)` | `nn.BCEWithLogitsLoss()` |\n| regression | numeric value | `nn.MSELoss()` or `nn.L1Loss()` |\n\nOptimizers:\n\n```python\noptimizer = torch.optim.AdamW(model.parameters(), lr=3e-4, weight_decay=1e-2)\nloss_fn = nn.CrossEntropyLoss()\n```\n\n**Do not apply softmax before `CrossEntropyLoss`.** It expects raw logits and applies log-softmax internally.\n\n## Saving and Loading Models\n\nRecommended pattern:\n\n```python\ntorch.save({\n    \"model_state\": model.state_dict(),\n    \"optimizer_state\": optimizer.state_dict(),\n    \"epoch\": epoch,\n    \"config\": config,\n}, \"checkpoint.pt\")\n```\n\nLoading:\n\n```python\ncheckpoint = torch.load(\"checkpoint.pt\", map_location=device)\nmodel.load_state_dict(checkpoint[\"model_state\"])\noptimizer.load_state_dict(checkpoint[\"optimizer_state\"])\n```\n\nFor inference-only:\n\n```python\nmodel.eval()\nwith torch.no_grad():\n    output = model(x)\n```\n\nSave model architecture separately in code/config. `state_dict` stores weights, not the class definition.\n\n## Mini CNN Example\n\n```python\nimport torch\nfrom torch import nn\n\nclass SmallCNN(nn.Module):\n    def __init__(self, num_classes=10):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(3, 32, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n\n            nn.Conv2d(32, 64, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n        )\n        self.classifier = nn.Sequential(\n            nn.Flatten(),\n            nn.Linear(64 * 8 * 8, 128),\n            nn.ReLU(),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        x = self.features(x)\n        return self.classifier(x)\n```\n\nFor `32x32` input images, two `MaxPool2d(2)` operations reduce spatial size from `32 -> 16 -> 8`.\n\n## Performance and Production Notes\n\nUseful practices:\n\n- use batches, not single samples\n- use `pin_memory=True` for CUDA DataLoaders where helpful\n- tune `num_workers`\n- use mixed precision for GPU training when appropriate\n- monitor GPU utilization\n- avoid unnecessary CPU-GPU transfers\n- profile before optimizing\n- save checkpoints regularly\n- seed randomness for reproducibility experiments\n\nExample mixed precision sketch:\n\n```python\nscaler = torch.cuda.amp.GradScaler()\n\nfor x, y in loader:\n    optimizer.zero_grad(set_to_none=True)\n    with torch.cuda.amp.autocast():\n        loss = loss_fn(model(x), y)\n    scaler.scale(loss).backward()\n    scaler.step(optimizer)\n    scaler.update()\n```\n\n## Common Mistakes\n\n| Mistake | Result |\n|---|---|\n| model on GPU, tensors on CPU | device mismatch error |\n| forgetting `zero_grad()` | gradient accumulation |\n| using softmax before CrossEntropyLoss | unstable/wrong training |\n| not using `model.eval()` | dropout/batchnorm behave like training |\n| not using `no_grad()` in validation | unnecessary memory usage |\n| wrong tensor shape | linear/conv dimension errors |\n| shuffling validation/test data unnecessarily | harder reproducibility/debugging |\n\n## Interview Questions and Answers\n\n**Q: What is autograd?**  \nA: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during `backward()`.\n\n**Q: What is `nn.Module`?**  \nA: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts.\n\n**Q: Why call `model.eval()`?**  \nA: It changes behavior of layers like dropout and batch normalization for inference/evaluation.\n\n**Q: What is a DataLoader?**  \nA: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.\n\n",
      "body_markdown": "# PyTorch: Tensors, Autograd, nn.Module, DataLoaders, Training Loops, and Production ML Patterns\n\nPyTorch provides tensor computation, automatic differentiation, neural network abstractions, and GPU acceleration. These notes explain how to build, train, debug, and reason about models in a practical engineering style.\n\n## What PyTorch Solves\n\nPyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration.\n\nAt a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning.\n\n```mermaid\nflowchart LR\n    Data[Dataset] --> Loader[DataLoader]\n    Loader --> Batch[Mini-batches]\n    Batch --> Model[nn.Module]\n    Model --> Pred[Predictions]\n    Pred --> Loss[Loss Function]\n    Loss --> Backward[loss.backward()]\n    Backward --> Optim[optimizer.step()]\n    Optim --> Model\n```\n\n**Mental model:** PyTorch training is repeated numeric transformation plus gradient-based parameter updates.\n\n## Tensors\n\nA tensor is a multi-dimensional array used to store inputs, outputs, labels, and model parameters.\n\n```python\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]])\nw = torch.randn(2, 3)\n\nprint(x.shape)   # torch.Size([2, 2])\nprint(w.dtype)   # torch.float32 by default\nprint(w.device)  # cpu or cuda/mps/xpu\n```\n\nCommon tensor dimensions:\n\n| Shape | Meaning |\n|---|---|\n| `()` | scalar |\n| `(features,)` | single vector |\n| `(batch, features)` | tabular batch |\n| `(batch, channels, height, width)` | image batch |\n| `(batch, sequence, embedding)` | NLP sequence batch |\n\n**Common mistake:** shape mismatch. Always print shapes at model boundaries.\n\n## Devices: CPU, CUDA, MPS\n\nModels and tensors must be on the same device.\n\n```python\nimport torch\n\ndevice = (\n    \"cuda\" if torch.cuda.is_available()\n    else \"mps\" if torch.backends.mps.is_available()\n    else \"cpu\"\n)\n\nmodel = model.to(device)\nx = x.to(device)\ny = y.to(device)\n```\n\nIf model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error.\n\n**Interview explanation:** GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.\n\n## Autograd and Computation Graphs\n\nAutograd tracks operations on tensors with `requires_grad=True` and computes gradients during backpropagation.\n\n```python\nimport torch\n\nx = torch.tensor([2.0], requires_grad=True)\ny = x ** 2 + 3 * x + 1\ny.backward()\n\nprint(x.grad)  # dy/dx = 2x + 3 = 7\n```\n\n```mermaid\nflowchart LR\n    X[x requires_grad] --> POW[x^2]\n    X --> MUL[3x]\n    POW --> SUM[y]\n    MUL --> SUM\n    SUM --> BACK[backward]\n    BACK --> GRAD[x.grad]\n```\n\nAutograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.\n\n## nn.Module\n\n`nn.Module` is the base class for neural networks. A model usually defines layers in `__init__` and computation in `forward`.\n\n```python\nfrom torch import nn\n\nclass MLP(nn.Module):\n    def __init__(self, in_features: int, num_classes: int):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(in_features, 128),\n            nn.ReLU(),\n            nn.Dropout(0.2),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n```\n\nWhy subclass `nn.Module`?\n\n- parameters are registered automatically\n- `.to(device)` moves all parameters\n- `.train()` and `.eval()` switch behavior of dropout/batchnorm\n- `state_dict()` enables saving/loading\n\n## Datasets and DataLoaders\n\nA `Dataset` knows how to access one sample. A `DataLoader` batches, shuffles, and parallelizes loading.\n\n```python\nfrom torch.utils.data import Dataset, DataLoader\n\nclass NotesDataset(Dataset):\n    def __init__(self, rows, tokenizer):\n        self.rows = rows\n        self.tokenizer = tokenizer\n\n    def __len__(self):\n        return len(self.rows)\n\n    def __getitem__(self, idx):\n        text = self.rows[idx][\"text\"]\n        label = self.rows[idx][\"label\"]\n        encoded = self.tokenizer(text)\n        return encoded, label\n\nloader = DataLoader(\n    NotesDataset(rows, tokenizer),\n    batch_size=32,\n    shuffle=True,\n    num_workers=4,\n)\n```\n\n**Good separation:** dataset handles sample access; model handles prediction; training loop handles optimization.\n\n## Training Loop\n\nA basic supervised training loop:\n\n```python\nimport torch\nfrom torch import nn\n\ndef train_one_epoch(model, loader, optimizer, loss_fn, device):\n    model.train()\n    total_loss = 0.0\n\n    for x, y in loader:\n        x = x.to(device)\n        y = y.to(device)\n\n        optimizer.zero_grad(set_to_none=True)\n        logits = model(x)\n        loss = loss_fn(logits, y)\n        loss.backward()\n        optimizer.step()\n\n        total_loss += loss.item()\n\n    return total_loss / len(loader)\n```\n\nOrder matters:\n\n```txt\nzero gradients -> forward -> compute loss -> backward -> optimizer step\n```\n\nIf you forget `optimizer.zero_grad()`, gradients accumulate across batches.\n\n## Evaluation Loop\n\nEvaluation should disable gradient tracking and switch the model to eval mode.\n\n```python\ndef evaluate(model, loader, loss_fn, device):\n    model.eval()\n    correct = 0\n    total = 0\n    total_loss = 0.0\n\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(device), y.to(device)\n            logits = model(x)\n            loss = loss_fn(logits, y)\n            total_loss += loss.item()\n\n            preds = logits.argmax(dim=1)\n            correct += (preds == y).sum().item()\n            total += y.numel()\n\n    return {\n        \"loss\": total_loss / len(loader),\n        \"accuracy\": correct / total,\n    }\n```\n\n`torch.no_grad()` reduces memory use because PyTorch does not store intermediate tensors for gradients.\n\n## Loss Functions and Optimizers\n\nCommon pairings:\n\n| Task | Output | Loss |\n|---|---|---|\n| multi-class classification | raw logits `(N, C)` | `nn.CrossEntropyLoss()` |\n| binary classification | logits `(N, 1)` | `nn.BCEWithLogitsLoss()` |\n| regression | numeric value | `nn.MSELoss()` or `nn.L1Loss()` |\n\nOptimizers:\n\n```python\noptimizer = torch.optim.AdamW(model.parameters(), lr=3e-4, weight_decay=1e-2)\nloss_fn = nn.CrossEntropyLoss()\n```\n\n**Do not apply softmax before `CrossEntropyLoss`.** It expects raw logits and applies log-softmax internally.\n\n## Saving and Loading Models\n\nRecommended pattern:\n\n```python\ntorch.save({\n    \"model_state\": model.state_dict(),\n    \"optimizer_state\": optimizer.state_dict(),\n    \"epoch\": epoch,\n    \"config\": config,\n}, \"checkpoint.pt\")\n```\n\nLoading:\n\n```python\ncheckpoint = torch.load(\"checkpoint.pt\", map_location=device)\nmodel.load_state_dict(checkpoint[\"model_state\"])\noptimizer.load_state_dict(checkpoint[\"optimizer_state\"])\n```\n\nFor inference-only:\n\n```python\nmodel.eval()\nwith torch.no_grad():\n    output = model(x)\n```\n\nSave model architecture separately in code/config. `state_dict` stores weights, not the class definition.\n\n## Mini CNN Example\n\n```python\nimport torch\nfrom torch import nn\n\nclass SmallCNN(nn.Module):\n    def __init__(self, num_classes=10):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(3, 32, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n\n            nn.Conv2d(32, 64, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n        )\n        self.classifier = nn.Sequential(\n            nn.Flatten(),\n            nn.Linear(64 * 8 * 8, 128),\n            nn.ReLU(),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        x = self.features(x)\n        return self.classifier(x)\n```\n\nFor `32x32` input images, two `MaxPool2d(2)` operations reduce spatial size from `32 -> 16 -> 8`.\n\n## Performance and Production Notes\n\nUseful practices:\n\n- use batches, not single samples\n- use `pin_memory=True` for CUDA DataLoaders where helpful\n- tune `num_workers`\n- use mixed precision for GPU training when appropriate\n- monitor GPU utilization\n- avoid unnecessary CPU-GPU transfers\n- profile before optimizing\n- save checkpoints regularly\n- seed randomness for reproducibility experiments\n\nExample mixed precision sketch:\n\n```python\nscaler = torch.cuda.amp.GradScaler()\n\nfor x, y in loader:\n    optimizer.zero_grad(set_to_none=True)\n    with torch.cuda.amp.autocast():\n        loss = loss_fn(model(x), y)\n    scaler.scale(loss).backward()\n    scaler.step(optimizer)\n    scaler.update()\n```\n\n## Common Mistakes\n\n| Mistake | Result |\n|---|---|\n| model on GPU, tensors on CPU | device mismatch error |\n| forgetting `zero_grad()` | gradient accumulation |\n| using softmax before CrossEntropyLoss | unstable/wrong training |\n| not using `model.eval()` | dropout/batchnorm behave like training |\n| not using `no_grad()` in validation | unnecessary memory usage |\n| wrong tensor shape | linear/conv dimension errors |\n| shuffling validation/test data unnecessarily | harder reproducibility/debugging |\n\n## Interview Questions and Answers\n\n**Q: What is autograd?**  \nA: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during `backward()`.\n\n**Q: What is `nn.Module`?**  \nA: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts.\n\n**Q: Why call `model.eval()`?**  \nA: It changes behavior of layers like dropout and batch normalization for inference/evaluation.\n\n**Q: What is a DataLoader?**  \nA: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.\n\n",
      "sections": [
        {
          "id": "pytorch-deep-dive__what-pytorch-solves",
          "slug": "what-pytorch-solves",
          "anchor": "pytorch-deep-dive__what-pytorch-solves",
          "title": "What PyTorch Solves",
          "level": 1,
          "order_index": 1,
          "parent_id": null,
          "content": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration.\n\nAt a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning.\n\n```mermaid\nflowchart LR\n    Data[Dataset] --> Loader[DataLoader]\n    Loader --> Batch[Mini-batches]\n    Batch --> Model[nn.Module]\n    Model --> Pred[Predictions]\n    Pred --> Loss[Loss Function]\n    Loss --> Backward[loss.backward()]\n    Backward --> Optim[optimizer.step()]\n    Optim --> Model\n```\n\n**Mental model:** PyTorch training is repeated numeric transformation plus gradient-based parameter updates.\n",
          "body_markdown": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration.\n\nAt a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning.\n\n```mermaid\nflowchart LR\n    Data[Dataset] --> Loader[DataLoader]\n    Loader --> Batch[Mini-batches]\n    Batch --> Model[nn.Module]\n    Model --> Pred[Predictions]\n    Pred --> Loss[Loss Function]\n    Loss --> Backward[loss.backward()]\n    Backward --> Optim[optimizer.step()]\n    Optim --> Model\n```\n\n**Mental model:** PyTorch training is repeated numeric transformation plus gradient-based parameter updates.\n",
          "rawText": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration. At a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning. code block Mental model: PyTorch training is repeated numeric transformation plus gradient based parameter updates.",
          "raw_text": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration. At a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning. code block Mental model: PyTorch training is repeated numeric transformation plus gradient based parameter updates.",
          "plainText": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration. At a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning. code block Mental model: PyTorch training is repeated numeric transformation plus gradient based parameter updates.",
          "plain_text": "PyTorch is a deep learning framework built around tensors, automatic differentiation, neural network modules, optimizers, and GPU acceleration. At a high level, PyTorch lets you write normal Python code while still building differentiable computation graphs for machine learning. code block Mental model: PyTorch training is repeated numeric transformation plus gradient based parameter updates.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__tensors",
          "slug": "tensors",
          "anchor": "pytorch-deep-dive__tensors",
          "title": "Tensors",
          "level": 1,
          "order_index": 2,
          "parent_id": null,
          "content": "A tensor is a multi-dimensional array used to store inputs, outputs, labels, and model parameters.\n\n```python\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]])\nw = torch.randn(2, 3)\n\nprint(x.shape)   # torch.Size([2, 2])\nprint(w.dtype)   # torch.float32 by default\nprint(w.device)  # cpu or cuda/mps/xpu\n```\n\nCommon tensor dimensions:\n\n| Shape | Meaning |\n|---|---|\n| `()` | scalar |\n| `(features,)` | single vector |\n| `(batch, features)` | tabular batch |\n| `(batch, channels, height, width)` | image batch |\n| `(batch, sequence, embedding)` | NLP sequence batch |\n\n**Common mistake:** shape mismatch. Always print shapes at model boundaries.\n",
          "body_markdown": "A tensor is a multi-dimensional array used to store inputs, outputs, labels, and model parameters.\n\n```python\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]])\nw = torch.randn(2, 3)\n\nprint(x.shape)   # torch.Size([2, 2])\nprint(w.dtype)   # torch.float32 by default\nprint(w.device)  # cpu or cuda/mps/xpu\n```\n\nCommon tensor dimensions:\n\n| Shape | Meaning |\n|---|---|\n| `()` | scalar |\n| `(features,)` | single vector |\n| `(batch, features)` | tabular batch |\n| `(batch, channels, height, width)` | image batch |\n| `(batch, sequence, embedding)` | NLP sequence batch |\n\n**Common mistake:** shape mismatch. Always print shapes at model boundaries.\n",
          "rawText": "A tensor is a multi dimensional array used to store inputs, outputs, labels, and model parameters. code block Common tensor dimensions: Shape Meaning () scalar (features,) single vector (batch, features) tabular batch (batch, channels, height, width) image batch (batch, sequence, embedding) NLP sequence batch Common mistake: shape mismatch. Always print shapes at model boundaries.",
          "raw_text": "A tensor is a multi dimensional array used to store inputs, outputs, labels, and model parameters. code block Common tensor dimensions: Shape Meaning () scalar (features,) single vector (batch, features) tabular batch (batch, channels, height, width) image batch (batch, sequence, embedding) NLP sequence batch Common mistake: shape mismatch. Always print shapes at model boundaries.",
          "plainText": "A tensor is a multi dimensional array used to store inputs, outputs, labels, and model parameters. code block Common tensor dimensions: Shape Meaning () scalar (features,) single vector (batch, features) tabular batch (batch, channels, height, width) image batch (batch, sequence, embedding) NLP sequence batch Common mistake: shape mismatch. Always print shapes at model boundaries.",
          "plain_text": "A tensor is a multi dimensional array used to store inputs, outputs, labels, and model parameters. code block Common tensor dimensions: Shape Meaning () scalar (features,) single vector (batch, features) tabular batch (batch, channels, height, width) image batch (batch, sequence, embedding) NLP sequence batch Common mistake: shape mismatch. Always print shapes at model boundaries.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__devices-cpu-cuda-mps",
          "slug": "devices-cpu-cuda-mps",
          "anchor": "pytorch-deep-dive__devices-cpu-cuda-mps",
          "title": "Devices: CPU, CUDA, MPS",
          "level": 1,
          "order_index": 3,
          "parent_id": null,
          "content": "Models and tensors must be on the same device.\n\n```python\nimport torch\n\ndevice = (\n    \"cuda\" if torch.cuda.is_available()\n    else \"mps\" if torch.backends.mps.is_available()\n    else \"cpu\"\n)\n\nmodel = model.to(device)\nx = x.to(device)\ny = y.to(device)\n```\n\nIf model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error.\n\n**Interview explanation:** GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.\n",
          "body_markdown": "Models and tensors must be on the same device.\n\n```python\nimport torch\n\ndevice = (\n    \"cuda\" if torch.cuda.is_available()\n    else \"mps\" if torch.backends.mps.is_available()\n    else \"cpu\"\n)\n\nmodel = model.to(device)\nx = x.to(device)\ny = y.to(device)\n```\n\nIf model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error.\n\n**Interview explanation:** GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.\n",
          "rawText": "Models and tensors must be on the same device. code block If model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error. Interview explanation: GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.",
          "raw_text": "Models and tensors must be on the same device. code block If model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error. Interview explanation: GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.",
          "plainText": "Models and tensors must be on the same device. code block If model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error. Interview explanation: GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.",
          "plain_text": "Models and tensors must be on the same device. code block If model is on GPU and tensors are on CPU, PyTorch raises a device mismatch error. Interview explanation: GPUs accelerate tensor operations through massive parallelism, but data transfer between CPU and GPU has overhead. Move data deliberately.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__autograd-and-computation-graphs",
          "slug": "autograd-and-computation-graphs",
          "anchor": "pytorch-deep-dive__autograd-and-computation-graphs",
          "title": "Autograd and Computation Graphs",
          "level": 1,
          "order_index": 4,
          "parent_id": null,
          "content": "Autograd tracks operations on tensors with `requires_grad=True` and computes gradients during backpropagation.\n\n```python\nimport torch\n\nx = torch.tensor([2.0], requires_grad=True)\ny = x ** 2 + 3 * x + 1\ny.backward()\n\nprint(x.grad)  # dy/dx = 2x + 3 = 7\n```\n\n```mermaid\nflowchart LR\n    X[x requires_grad] --> POW[x^2]\n    X --> MUL[3x]\n    POW --> SUM[y]\n    MUL --> SUM\n    SUM --> BACK[backward]\n    BACK --> GRAD[x.grad]\n```\n\nAutograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.\n",
          "body_markdown": "Autograd tracks operations on tensors with `requires_grad=True` and computes gradients during backpropagation.\n\n```python\nimport torch\n\nx = torch.tensor([2.0], requires_grad=True)\ny = x ** 2 + 3 * x + 1\ny.backward()\n\nprint(x.grad)  # dy/dx = 2x + 3 = 7\n```\n\n```mermaid\nflowchart LR\n    X[x requires_grad] --> POW[x^2]\n    X --> MUL[3x]\n    POW --> SUM[y]\n    MUL --> SUM\n    SUM --> BACK[backward]\n    BACK --> GRAD[x.grad]\n```\n\nAutograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.\n",
          "rawText": "Autograd tracks operations on tensors with requires grad=True and computes gradients during backpropagation. code block code block Autograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.",
          "raw_text": "Autograd tracks operations on tensors with requires grad=True and computes gradients during backpropagation. code block code block Autograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.",
          "plainText": "Autograd tracks operations on tensors with requires grad=True and computes gradients during backpropagation. code block code block Autograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.",
          "plain_text": "Autograd tracks operations on tensors with requires grad=True and computes gradients during backpropagation. code block code block Autograd builds a dynamic graph at runtime. This makes debugging easier because the graph follows normal Python control flow.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__nn-module",
          "slug": "nn-module",
          "anchor": "pytorch-deep-dive__nn-module",
          "title": "nn.Module",
          "level": 1,
          "order_index": 5,
          "parent_id": null,
          "content": "`nn.Module` is the base class for neural networks. A model usually defines layers in `__init__` and computation in `forward`.\n\n```python\nfrom torch import nn\n\nclass MLP(nn.Module):\n    def __init__(self, in_features: int, num_classes: int):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(in_features, 128),\n            nn.ReLU(),\n            nn.Dropout(0.2),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n```\n\nWhy subclass `nn.Module`?\n\n- parameters are registered automatically\n- `.to(device)` moves all parameters\n- `.train()` and `.eval()` switch behavior of dropout/batchnorm\n- `state_dict()` enables saving/loading\n",
          "body_markdown": "`nn.Module` is the base class for neural networks. A model usually defines layers in `__init__` and computation in `forward`.\n\n```python\nfrom torch import nn\n\nclass MLP(nn.Module):\n    def __init__(self, in_features: int, num_classes: int):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(in_features, 128),\n            nn.ReLU(),\n            nn.Dropout(0.2),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n```\n\nWhy subclass `nn.Module`?\n\n- parameters are registered automatically\n- `.to(device)` moves all parameters\n- `.train()` and `.eval()` switch behavior of dropout/batchnorm\n- `state_dict()` enables saving/loading\n",
          "rawText": "nn.Module is the base class for neural networks. A model usually defines layers in init and computation in forward. code block Why subclass nn.Module? parameters are registered automatically .to(device) moves all parameters .train() and .eval() switch behavior of dropout/batchnorm state dict() enables saving/loading",
          "raw_text": "nn.Module is the base class for neural networks. A model usually defines layers in init and computation in forward. code block Why subclass nn.Module? parameters are registered automatically .to(device) moves all parameters .train() and .eval() switch behavior of dropout/batchnorm state dict() enables saving/loading",
          "plainText": "nn.Module is the base class for neural networks. A model usually defines layers in init and computation in forward. code block Why subclass nn.Module? parameters are registered automatically .to(device) moves all parameters .train() and .eval() switch behavior of dropout/batchnorm state dict() enables saving/loading",
          "plain_text": "nn.Module is the base class for neural networks. A model usually defines layers in init and computation in forward. code block Why subclass nn.Module? parameters are registered automatically .to(device) moves all parameters .train() and .eval() switch behavior of dropout/batchnorm state dict() enables saving/loading",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__datasets-and-dataloaders",
          "slug": "datasets-and-dataloaders",
          "anchor": "pytorch-deep-dive__datasets-and-dataloaders",
          "title": "Datasets and DataLoaders",
          "level": 1,
          "order_index": 6,
          "parent_id": null,
          "content": "A `Dataset` knows how to access one sample. A `DataLoader` batches, shuffles, and parallelizes loading.\n\n```python\nfrom torch.utils.data import Dataset, DataLoader\n\nclass NotesDataset(Dataset):\n    def __init__(self, rows, tokenizer):\n        self.rows = rows\n        self.tokenizer = tokenizer\n\n    def __len__(self):\n        return len(self.rows)\n\n    def __getitem__(self, idx):\n        text = self.rows[idx][\"text\"]\n        label = self.rows[idx][\"label\"]\n        encoded = self.tokenizer(text)\n        return encoded, label\n\nloader = DataLoader(\n    NotesDataset(rows, tokenizer),\n    batch_size=32,\n    shuffle=True,\n    num_workers=4,\n)\n```\n\n**Good separation:** dataset handles sample access; model handles prediction; training loop handles optimization.\n",
          "body_markdown": "A `Dataset` knows how to access one sample. A `DataLoader` batches, shuffles, and parallelizes loading.\n\n```python\nfrom torch.utils.data import Dataset, DataLoader\n\nclass NotesDataset(Dataset):\n    def __init__(self, rows, tokenizer):\n        self.rows = rows\n        self.tokenizer = tokenizer\n\n    def __len__(self):\n        return len(self.rows)\n\n    def __getitem__(self, idx):\n        text = self.rows[idx][\"text\"]\n        label = self.rows[idx][\"label\"]\n        encoded = self.tokenizer(text)\n        return encoded, label\n\nloader = DataLoader(\n    NotesDataset(rows, tokenizer),\n    batch_size=32,\n    shuffle=True,\n    num_workers=4,\n)\n```\n\n**Good separation:** dataset handles sample access; model handles prediction; training loop handles optimization.\n",
          "rawText": "A Dataset knows how to access one sample. A DataLoader batches, shuffles, and parallelizes loading. code block Good separation: dataset handles sample access; model handles prediction; training loop handles optimization.",
          "raw_text": "A Dataset knows how to access one sample. A DataLoader batches, shuffles, and parallelizes loading. code block Good separation: dataset handles sample access; model handles prediction; training loop handles optimization.",
          "plainText": "A Dataset knows how to access one sample. A DataLoader batches, shuffles, and parallelizes loading. code block Good separation: dataset handles sample access; model handles prediction; training loop handles optimization.",
          "plain_text": "A Dataset knows how to access one sample. A DataLoader batches, shuffles, and parallelizes loading. code block Good separation: dataset handles sample access; model handles prediction; training loop handles optimization.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__training-loop",
          "slug": "training-loop",
          "anchor": "pytorch-deep-dive__training-loop",
          "title": "Training Loop",
          "level": 1,
          "order_index": 7,
          "parent_id": null,
          "content": "A basic supervised training loop:\n\n```python\nimport torch\nfrom torch import nn\n\ndef train_one_epoch(model, loader, optimizer, loss_fn, device):\n    model.train()\n    total_loss = 0.0\n\n    for x, y in loader:\n        x = x.to(device)\n        y = y.to(device)\n\n        optimizer.zero_grad(set_to_none=True)\n        logits = model(x)\n        loss = loss_fn(logits, y)\n        loss.backward()\n        optimizer.step()\n\n        total_loss += loss.item()\n\n    return total_loss / len(loader)\n```\n\nOrder matters:\n\n```txt\nzero gradients -> forward -> compute loss -> backward -> optimizer step\n```\n\nIf you forget `optimizer.zero_grad()`, gradients accumulate across batches.\n",
          "body_markdown": "A basic supervised training loop:\n\n```python\nimport torch\nfrom torch import nn\n\ndef train_one_epoch(model, loader, optimizer, loss_fn, device):\n    model.train()\n    total_loss = 0.0\n\n    for x, y in loader:\n        x = x.to(device)\n        y = y.to(device)\n\n        optimizer.zero_grad(set_to_none=True)\n        logits = model(x)\n        loss = loss_fn(logits, y)\n        loss.backward()\n        optimizer.step()\n\n        total_loss += loss.item()\n\n    return total_loss / len(loader)\n```\n\nOrder matters:\n\n```txt\nzero gradients -> forward -> compute loss -> backward -> optimizer step\n```\n\nIf you forget `optimizer.zero_grad()`, gradients accumulate across batches.\n",
          "rawText": "A basic supervised training loop: code block Order matters: code block If you forget optimizer.zero grad(), gradients accumulate across batches.",
          "raw_text": "A basic supervised training loop: code block Order matters: code block If you forget optimizer.zero grad(), gradients accumulate across batches.",
          "plainText": "A basic supervised training loop: code block Order matters: code block If you forget optimizer.zero grad(), gradients accumulate across batches.",
          "plain_text": "A basic supervised training loop: code block Order matters: code block If you forget optimizer.zero grad(), gradients accumulate across batches.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__evaluation-loop",
          "slug": "evaluation-loop",
          "anchor": "pytorch-deep-dive__evaluation-loop",
          "title": "Evaluation Loop",
          "level": 1,
          "order_index": 8,
          "parent_id": null,
          "content": "Evaluation should disable gradient tracking and switch the model to eval mode.\n\n```python\ndef evaluate(model, loader, loss_fn, device):\n    model.eval()\n    correct = 0\n    total = 0\n    total_loss = 0.0\n\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(device), y.to(device)\n            logits = model(x)\n            loss = loss_fn(logits, y)\n            total_loss += loss.item()\n\n            preds = logits.argmax(dim=1)\n            correct += (preds == y).sum().item()\n            total += y.numel()\n\n    return {\n        \"loss\": total_loss / len(loader),\n        \"accuracy\": correct / total,\n    }\n```\n\n`torch.no_grad()` reduces memory use because PyTorch does not store intermediate tensors for gradients.\n",
          "body_markdown": "Evaluation should disable gradient tracking and switch the model to eval mode.\n\n```python\ndef evaluate(model, loader, loss_fn, device):\n    model.eval()\n    correct = 0\n    total = 0\n    total_loss = 0.0\n\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(device), y.to(device)\n            logits = model(x)\n            loss = loss_fn(logits, y)\n            total_loss += loss.item()\n\n            preds = logits.argmax(dim=1)\n            correct += (preds == y).sum().item()\n            total += y.numel()\n\n    return {\n        \"loss\": total_loss / len(loader),\n        \"accuracy\": correct / total,\n    }\n```\n\n`torch.no_grad()` reduces memory use because PyTorch does not store intermediate tensors for gradients.\n",
          "rawText": "Evaluation should disable gradient tracking and switch the model to eval mode. code block torch.no grad() reduces memory use because PyTorch does not store intermediate tensors for gradients.",
          "raw_text": "Evaluation should disable gradient tracking and switch the model to eval mode. code block torch.no grad() reduces memory use because PyTorch does not store intermediate tensors for gradients.",
          "plainText": "Evaluation should disable gradient tracking and switch the model to eval mode. code block torch.no grad() reduces memory use because PyTorch does not store intermediate tensors for gradients.",
          "plain_text": "Evaluation should disable gradient tracking and switch the model to eval mode. code block torch.no grad() reduces memory use because PyTorch does not store intermediate tensors for gradients.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__loss-functions-and-optimizers",
          "slug": "loss-functions-and-optimizers",
          "anchor": "pytorch-deep-dive__loss-functions-and-optimizers",
          "title": "Loss Functions and Optimizers",
          "level": 1,
          "order_index": 9,
          "parent_id": null,
          "content": "Common pairings:\n\n| Task | Output | Loss |\n|---|---|---|\n| multi-class classification | raw logits `(N, C)` | `nn.CrossEntropyLoss()` |\n| binary classification | logits `(N, 1)` | `nn.BCEWithLogitsLoss()` |\n| regression | numeric value | `nn.MSELoss()` or `nn.L1Loss()` |\n\nOptimizers:\n\n```python\noptimizer = torch.optim.AdamW(model.parameters(), lr=3e-4, weight_decay=1e-2)\nloss_fn = nn.CrossEntropyLoss()\n```\n\n**Do not apply softmax before `CrossEntropyLoss`.** It expects raw logits and applies log-softmax internally.\n",
          "body_markdown": "Common pairings:\n\n| Task | Output | Loss |\n|---|---|---|\n| multi-class classification | raw logits `(N, C)` | `nn.CrossEntropyLoss()` |\n| binary classification | logits `(N, 1)` | `nn.BCEWithLogitsLoss()` |\n| regression | numeric value | `nn.MSELoss()` or `nn.L1Loss()` |\n\nOptimizers:\n\n```python\noptimizer = torch.optim.AdamW(model.parameters(), lr=3e-4, weight_decay=1e-2)\nloss_fn = nn.CrossEntropyLoss()\n```\n\n**Do not apply softmax before `CrossEntropyLoss`.** It expects raw logits and applies log-softmax internally.\n",
          "rawText": "Common pairings: Task Output Loss multi class classification raw logits (N, C) nn.CrossEntropyLoss() binary classification logits (N, 1) nn.BCEWithLogitsLoss() regression numeric value nn.MSELoss() or nn.L1Loss() Optimizers: code block Do not apply softmax before CrossEntropyLoss. It expects raw logits and applies log softmax internally.",
          "raw_text": "Common pairings: Task Output Loss multi class classification raw logits (N, C) nn.CrossEntropyLoss() binary classification logits (N, 1) nn.BCEWithLogitsLoss() regression numeric value nn.MSELoss() or nn.L1Loss() Optimizers: code block Do not apply softmax before CrossEntropyLoss. It expects raw logits and applies log softmax internally.",
          "plainText": "Common pairings: Task Output Loss multi class classification raw logits (N, C) nn.CrossEntropyLoss() binary classification logits (N, 1) nn.BCEWithLogitsLoss() regression numeric value nn.MSELoss() or nn.L1Loss() Optimizers: code block Do not apply softmax before CrossEntropyLoss. It expects raw logits and applies log softmax internally.",
          "plain_text": "Common pairings: Task Output Loss multi class classification raw logits (N, C) nn.CrossEntropyLoss() binary classification logits (N, 1) nn.BCEWithLogitsLoss() regression numeric value nn.MSELoss() or nn.L1Loss() Optimizers: code block Do not apply softmax before CrossEntropyLoss. It expects raw logits and applies log softmax internally.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__saving-and-loading-models",
          "slug": "saving-and-loading-models",
          "anchor": "pytorch-deep-dive__saving-and-loading-models",
          "title": "Saving and Loading Models",
          "level": 1,
          "order_index": 10,
          "parent_id": null,
          "content": "Recommended pattern:\n\n```python\ntorch.save({\n    \"model_state\": model.state_dict(),\n    \"optimizer_state\": optimizer.state_dict(),\n    \"epoch\": epoch,\n    \"config\": config,\n}, \"checkpoint.pt\")\n```\n\nLoading:\n\n```python\ncheckpoint = torch.load(\"checkpoint.pt\", map_location=device)\nmodel.load_state_dict(checkpoint[\"model_state\"])\noptimizer.load_state_dict(checkpoint[\"optimizer_state\"])\n```\n\nFor inference-only:\n\n```python\nmodel.eval()\nwith torch.no_grad():\n    output = model(x)\n```\n\nSave model architecture separately in code/config. `state_dict` stores weights, not the class definition.\n",
          "body_markdown": "Recommended pattern:\n\n```python\ntorch.save({\n    \"model_state\": model.state_dict(),\n    \"optimizer_state\": optimizer.state_dict(),\n    \"epoch\": epoch,\n    \"config\": config,\n}, \"checkpoint.pt\")\n```\n\nLoading:\n\n```python\ncheckpoint = torch.load(\"checkpoint.pt\", map_location=device)\nmodel.load_state_dict(checkpoint[\"model_state\"])\noptimizer.load_state_dict(checkpoint[\"optimizer_state\"])\n```\n\nFor inference-only:\n\n```python\nmodel.eval()\nwith torch.no_grad():\n    output = model(x)\n```\n\nSave model architecture separately in code/config. `state_dict` stores weights, not the class definition.\n",
          "rawText": "Recommended pattern: code block Loading: code block For inference only: code block Save model architecture separately in code/config. state dict stores weights, not the class definition.",
          "raw_text": "Recommended pattern: code block Loading: code block For inference only: code block Save model architecture separately in code/config. state dict stores weights, not the class definition.",
          "plainText": "Recommended pattern: code block Loading: code block For inference only: code block Save model architecture separately in code/config. state dict stores weights, not the class definition.",
          "plain_text": "Recommended pattern: code block Loading: code block For inference only: code block Save model architecture separately in code/config. state dict stores weights, not the class definition.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__mini-cnn-example",
          "slug": "mini-cnn-example",
          "anchor": "pytorch-deep-dive__mini-cnn-example",
          "title": "Mini CNN Example",
          "level": 1,
          "order_index": 11,
          "parent_id": null,
          "content": "```python\nimport torch\nfrom torch import nn\n\nclass SmallCNN(nn.Module):\n    def __init__(self, num_classes=10):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(3, 32, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n\n            nn.Conv2d(32, 64, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n        )\n        self.classifier = nn.Sequential(\n            nn.Flatten(),\n            nn.Linear(64 * 8 * 8, 128),\n            nn.ReLU(),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        x = self.features(x)\n        return self.classifier(x)\n```\n\nFor `32x32` input images, two `MaxPool2d(2)` operations reduce spatial size from `32 -> 16 -> 8`.\n",
          "body_markdown": "```python\nimport torch\nfrom torch import nn\n\nclass SmallCNN(nn.Module):\n    def __init__(self, num_classes=10):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(3, 32, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n\n            nn.Conv2d(32, 64, kernel_size=3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n        )\n        self.classifier = nn.Sequential(\n            nn.Flatten(),\n            nn.Linear(64 * 8 * 8, 128),\n            nn.ReLU(),\n            nn.Linear(128, num_classes),\n        )\n\n    def forward(self, x):\n        x = self.features(x)\n        return self.classifier(x)\n```\n\nFor `32x32` input images, two `MaxPool2d(2)` operations reduce spatial size from `32 -> 16 -> 8`.\n",
          "rawText": "code block For 32x32 input images, two MaxPool2d(2) operations reduce spatial size from 32 16 8.",
          "raw_text": "code block For 32x32 input images, two MaxPool2d(2) operations reduce spatial size from 32 16 8.",
          "plainText": "code block For 32x32 input images, two MaxPool2d(2) operations reduce spatial size from 32 16 8.",
          "plain_text": "code block For 32x32 input images, two MaxPool2d(2) operations reduce spatial size from 32 16 8.",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__performance-and-production-notes",
          "slug": "performance-and-production-notes",
          "anchor": "pytorch-deep-dive__performance-and-production-notes",
          "title": "Performance and Production Notes",
          "level": 1,
          "order_index": 12,
          "parent_id": null,
          "content": "Useful practices:\n\n- use batches, not single samples\n- use `pin_memory=True` for CUDA DataLoaders where helpful\n- tune `num_workers`\n- use mixed precision for GPU training when appropriate\n- monitor GPU utilization\n- avoid unnecessary CPU-GPU transfers\n- profile before optimizing\n- save checkpoints regularly\n- seed randomness for reproducibility experiments\n\nExample mixed precision sketch:\n\n```python\nscaler = torch.cuda.amp.GradScaler()\n\nfor x, y in loader:\n    optimizer.zero_grad(set_to_none=True)\n    with torch.cuda.amp.autocast():\n        loss = loss_fn(model(x), y)\n    scaler.scale(loss).backward()\n    scaler.step(optimizer)\n    scaler.update()\n```\n",
          "body_markdown": "Useful practices:\n\n- use batches, not single samples\n- use `pin_memory=True` for CUDA DataLoaders where helpful\n- tune `num_workers`\n- use mixed precision for GPU training when appropriate\n- monitor GPU utilization\n- avoid unnecessary CPU-GPU transfers\n- profile before optimizing\n- save checkpoints regularly\n- seed randomness for reproducibility experiments\n\nExample mixed precision sketch:\n\n```python\nscaler = torch.cuda.amp.GradScaler()\n\nfor x, y in loader:\n    optimizer.zero_grad(set_to_none=True)\n    with torch.cuda.amp.autocast():\n        loss = loss_fn(model(x), y)\n    scaler.scale(loss).backward()\n    scaler.step(optimizer)\n    scaler.update()\n```\n",
          "rawText": "Useful practices: use batches, not single samples use pin memory=True for CUDA DataLoaders where helpful tune num workers use mixed precision for GPU training when appropriate monitor GPU utilization avoid unnecessary CPU GPU transfers profile before optimizing save checkpoints regularly seed randomness for reproducibility experiments Example mixed precision sketch: code block",
          "raw_text": "Useful practices: use batches, not single samples use pin memory=True for CUDA DataLoaders where helpful tune num workers use mixed precision for GPU training when appropriate monitor GPU utilization avoid unnecessary CPU GPU transfers profile before optimizing save checkpoints regularly seed randomness for reproducibility experiments Example mixed precision sketch: code block",
          "plainText": "Useful practices: use batches, not single samples use pin memory=True for CUDA DataLoaders where helpful tune num workers use mixed precision for GPU training when appropriate monitor GPU utilization avoid unnecessary CPU GPU transfers profile before optimizing save checkpoints regularly seed randomness for reproducibility experiments Example mixed precision sketch: code block",
          "plain_text": "Useful practices: use batches, not single samples use pin memory=True for CUDA DataLoaders where helpful tune num workers use mixed precision for GPU training when appropriate monitor GPU utilization avoid unnecessary CPU GPU transfers profile before optimizing save checkpoints regularly seed randomness for reproducibility experiments Example mixed precision sketch: code block",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__common-mistakes",
          "slug": "common-mistakes",
          "anchor": "pytorch-deep-dive__common-mistakes",
          "title": "Common Mistakes",
          "level": 1,
          "order_index": 13,
          "parent_id": null,
          "content": "| Mistake | Result |\n|---|---|\n| model on GPU, tensors on CPU | device mismatch error |\n| forgetting `zero_grad()` | gradient accumulation |\n| using softmax before CrossEntropyLoss | unstable/wrong training |\n| not using `model.eval()` | dropout/batchnorm behave like training |\n| not using `no_grad()` in validation | unnecessary memory usage |\n| wrong tensor shape | linear/conv dimension errors |\n| shuffling validation/test data unnecessarily | harder reproducibility/debugging |\n",
          "body_markdown": "| Mistake | Result |\n|---|---|\n| model on GPU, tensors on CPU | device mismatch error |\n| forgetting `zero_grad()` | gradient accumulation |\n| using softmax before CrossEntropyLoss | unstable/wrong training |\n| not using `model.eval()` | dropout/batchnorm behave like training |\n| not using `no_grad()` in validation | unnecessary memory usage |\n| wrong tensor shape | linear/conv dimension errors |\n| shuffling validation/test data unnecessarily | harder reproducibility/debugging |\n",
          "rawText": "Mistake Result model on GPU, tensors on CPU device mismatch error forgetting zero grad() gradient accumulation using softmax before CrossEntropyLoss unstable/wrong training not using model.eval() dropout/batchnorm behave like training not using no grad() in validation unnecessary memory usage wrong tensor shape linear/conv dimension errors shuffling validation/test data unnecessarily harder reproducibility/debugging",
          "raw_text": "Mistake Result model on GPU, tensors on CPU device mismatch error forgetting zero grad() gradient accumulation using softmax before CrossEntropyLoss unstable/wrong training not using model.eval() dropout/batchnorm behave like training not using no grad() in validation unnecessary memory usage wrong tensor shape linear/conv dimension errors shuffling validation/test data unnecessarily harder reproducibility/debugging",
          "plainText": "Mistake Result model on GPU, tensors on CPU device mismatch error forgetting zero grad() gradient accumulation using softmax before CrossEntropyLoss unstable/wrong training not using model.eval() dropout/batchnorm behave like training not using no grad() in validation unnecessary memory usage wrong tensor shape linear/conv dimension errors shuffling validation/test data unnecessarily harder reproducibility/debugging",
          "plain_text": "Mistake Result model on GPU, tensors on CPU device mismatch error forgetting zero grad() gradient accumulation using softmax before CrossEntropyLoss unstable/wrong training not using model.eval() dropout/batchnorm behave like training not using no grad() in validation unnecessary memory usage wrong tensor shape linear/conv dimension errors shuffling validation/test data unnecessarily harder reproducibility/debugging",
          "kind": "section"
        },
        {
          "id": "pytorch-deep-dive__interview-questions-and-answers",
          "slug": "interview-questions-and-answers",
          "anchor": "pytorch-deep-dive__interview-questions-and-answers",
          "title": "Interview Questions and Answers",
          "level": 1,
          "order_index": 14,
          "parent_id": null,
          "content": "**Q: What is autograd?**  \nA: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during `backward()`.\n\n**Q: What is `nn.Module`?**  \nA: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts.\n\n**Q: Why call `model.eval()`?**  \nA: It changes behavior of layers like dropout and batch normalization for inference/evaluation.\n\n**Q: What is a DataLoader?**  \nA: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.\n",
          "body_markdown": "**Q: What is autograd?**  \nA: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during `backward()`.\n\n**Q: What is `nn.Module`?**  \nA: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts.\n\n**Q: Why call `model.eval()`?**  \nA: It changes behavior of layers like dropout and batch normalization for inference/evaluation.\n\n**Q: What is a DataLoader?**  \nA: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.\n",
          "rawText": "Q: What is autograd? A: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during backward(). Q: What is nn.Module? A: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts. Q: Why call model.eval()? A: It changes behavior of layers like dropout and batch normalization for inference/evaluation. Q: What is a DataLoader? A: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.",
          "raw_text": "Q: What is autograd? A: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during backward(). Q: What is nn.Module? A: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts. Q: Why call model.eval()? A: It changes behavior of layers like dropout and batch normalization for inference/evaluation. Q: What is a DataLoader? A: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.",
          "plainText": "Q: What is autograd? A: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during backward(). Q: What is nn.Module? A: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts. Q: Why call model.eval()? A: It changes behavior of layers like dropout and batch normalization for inference/evaluation. Q: What is a DataLoader? A: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.",
          "plain_text": "Q: What is autograd? A: PyTorch's automatic differentiation engine. It records tensor operations dynamically and computes gradients during backward(). Q: What is nn.Module? A: The base abstraction for trainable models/layers. It registers parameters, supports device movement, train/eval modes, and state dicts. Q: Why call model.eval()? A: It changes behavior of layers like dropout and batch normalization for inference/evaluation. Q: What is a DataLoader? A: It wraps a Dataset and provides batching, shuffling, multiprocessing, and iterable access to samples.",
          "kind": "section"
        }
      ],
      "tags": [
        "pytorch",
        "machine-learning",
        "deep-learning",
        "python",
        "ai"
      ],
      "sourceFiles": [
        "PyTorch Tutorials - Tensors",
        "PyTorch Tutorials - Datasets and DataLoaders",
        "PyTorch Docs - torch.nn.Module",
        "PyTorch Tutorials - Optimization"
      ],
      "source_keys": [
        "pytorch-tutorials-tensors",
        "pytorch-tutorials-datasets-and-dataloaders",
        "pytorch-docs-torch-nn-module",
        "pytorch-tutorials-optimization"
      ],
      "order_index": 840,
      "is_featured": true,
      "metadata": {
        "seed_version": "deep-additions-v1",
        "generated_at": "2026-06-27",
        "intended_endpoint": "/api/v1/admin/topics/bulk-upsert-fast?rebuild_bootstrap=true",
        "source_refs": [
          {
            "label": "PyTorch Tutorials - Tensors",
            "url": "https://docs.pytorch.org/tutorials/beginner/basics/tensorqs_tutorial.html"
          },
          {
            "label": "PyTorch Tutorials - Datasets and DataLoaders",
            "url": "https://docs.pytorch.org/tutorials/beginner/basics/data_tutorial.html"
          },
          {
            "label": "PyTorch Docs - torch.nn.Module",
            "url": "https://docs.pytorch.org/docs/stable/generated/torch.nn.Module.html"
          },
          {
            "label": "PyTorch Tutorials - Optimization",
            "url": "https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial.html"
          }
        ],
        "section_strategy": "flat sections with level + parent_id + stable anchors"
      },
      "extra": {
        "learning_outcomes": [
          "Explain the concept in interviews with production-grade clarity",
          "Recognize common mistakes and debug real issues",
          "Apply the topic with working code/configuration examples",
          "Use diagrams to build a mental model before memorizing syntax"
        ],
        "recommended_rendering": {
          "left_sidebar": "topic tree",
          "right_toc": "sections by anchor",
          "scrollspy_key": "section.anchor"
        }
      }
    }
  ]
}