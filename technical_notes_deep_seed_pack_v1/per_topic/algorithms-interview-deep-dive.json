{
  "mode": "upsert",
  "topics": [
    {
      "id": "algorithms-interview-deep-dive",
      "slug": "algorithms-interview-deep-dive",
      "title": "Algorithms Interview Notes: Binary Search, Two Pointers, Sliding Window, Sorting, Backtracking, Graphs, DP, Greedy, and Bit Manipulation",
      "subtitle": "Detailed algorithm notes with patterns, templates, Python implementations, complexity, diagrams, and interview explanations.",
      "summary": "Algorithms are repeatable ways to solve computational problems. These notes focus on pattern recognition, templates, correctness intuition, complexity, and interview-ready explanations.",
      "group": "Computer Science",
      "group_slug": "computer-science",
      "group_name": "Computer Science",
      "domain": "DSA",
      "domain_slug": "dsa",
      "domain_name": "DSA",
      "status": "published",
      "content_format": "markdown",
      "difficulty": "advanced",
      "content": "# Algorithms Interview Notes: Binary Search, Two Pointers, Sliding Window, Sorting, Backtracking, Graphs, DP, Greedy, and Bit Manipulation\n\nAlgorithms are repeatable ways to solve computational problems. These notes focus on pattern recognition, templates, correctness intuition, complexity, and interview-ready explanations.\n\n## How to Approach Algorithm Problems\n\nAlgorithmic thinking starts with classifying the problem.\n\n```mermaid\nflowchart TD\n    P[Problem] --> C{Pattern?}\n    C -->|Sorted / monotonic| BinarySearch\n    C -->|Subarray / substring| SlidingWindow\n    C -->|Pairs / partition| TwoPointers\n    C -->|Choices| Backtracking\n    C -->|Graph reachability| DFS_BFS\n    C -->|Weighted shortest path| Dijkstra\n    C -->|Optimal substructure| DP\n    C -->|Local best works| Greedy\n```\n\nInterview process:\n\n1. clarify input/output\n2. ask constraints\n3. identify brute force\n4. find bottleneck\n5. choose data structure/algorithm\n6. prove correctness informally\n7. analyze complexity\n8. test edge cases\n\n## Binary Search\n\nUse binary search when the answer space is sorted or monotonic.\n\nClassic template:\n\n```python\ndef binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n\n    while lo <= hi:\n        mid = lo + (hi - lo) // 2\n\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n\n    return -1\n```\n\nLower bound:\n\n```python\ndef lower_bound(nums, target):\n    lo, hi = 0, len(nums)\n\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n\n    return lo\n```\n\n**Key:** binary search is not only for arrays. It works on any monotonic predicate.\n\n## Two Pointers\n\nTwo pointers are useful when movement of one or both ends reduces search space.\n\nExample: pair sum in sorted array.\n\n```python\ndef has_pair_sum(nums, target):\n    left, right = 0, len(nums) - 1\n\n    while left < right:\n        s = nums[left] + nums[right]\n        if s == target:\n            return True\n        if s < target:\n            left += 1\n        else:\n            right -= 1\n\n    return False\n```\n\nCommon uses:\n\n- sorted pair problems\n- reversing\n- partitioning\n- removing duplicates\n- palindrome checks\n\n## Sliding Window\n\nSliding window is for contiguous subarray/substring problems.\n\nFixed window:\n\n```python\ndef max_sum_k(nums, k):\n    window = sum(nums[:k])\n    best = window\n\n    for right in range(k, len(nums)):\n        window += nums[right] - nums[right - k]\n        best = max(best, window)\n\n    return best\n```\n\nVariable window:\n\n```python\ndef longest_at_most_k_distinct(s, k):\n    from collections import defaultdict\n\n    freq = defaultdict(int)\n    left = 0\n    best = 0\n\n    for right, ch in enumerate(s):\n        freq[ch] += 1\n\n        while len(freq) > k:\n            old = s[left]\n            freq[old] -= 1\n            if freq[old] == 0:\n                del freq[old]\n            left += 1\n\n        best = max(best, right - left + 1)\n\n    return best\n```\n\n## Sorting Algorithms and When They Matter\n\nCommon sorting complexities:\n\n| Algorithm | Average | Worst | Stable |\n|---|---:|---:|---|\n| Merge sort | O(n log n) | O(n log n) | yes |\n| Quick sort | O(n log n) | O(n²) | usually no |\n| Heap sort | O(n log n) | O(n log n) | no |\n| Counting sort | O(n + k) | O(n + k) | yes possible |\n\nPython's built-in sort is Timsort and is stable.\n\nUse sorting when:\n\n- order reveals structure\n- intervals can be merged\n- greedy choice depends on ordering\n- duplicates grouping becomes easier\n\nMerge intervals:\n\n```python\ndef merge_intervals(intervals):\n    intervals.sort()\n    merged = []\n\n    for start, end in intervals:\n        if not merged or start > merged[-1][1]:\n            merged.append([start, end])\n        else:\n            merged[-1][1] = max(merged[-1][1], end)\n\n    return merged\n```\n\n## Recursion and Backtracking\n\nBacktracking explores choices and undoes them.\n\n```python\ndef subsets(nums):\n    res = []\n    path = []\n\n    def dfs(i):\n        if i == len(nums):\n            res.append(path.copy())\n            return\n\n        path.append(nums[i])\n        dfs(i + 1)\n\n        path.pop()\n        dfs(i + 1)\n\n    dfs(0)\n    return res\n```\n\nBacktracking framework:\n\n```txt\nchoose -> explore -> unchoose\n```\n\nUse for:\n\n- subsets\n- permutations\n- combinations\n- N-Queens\n- Sudoku\n- word search\n\n## BFS and DFS\n\nDFS goes deep; BFS expands level by level.\n\nDFS recursive:\n\n```python\ndef dfs(node, graph, seen):\n    if node in seen:\n        return\n\n    seen.add(node)\n\n    for nei in graph[node]:\n        dfs(nei, graph, seen)\n```\n\nBFS shortest path in unweighted graph:\n\n```python\nfrom collections import deque\n\ndef shortest_path_unweighted(graph, start, target):\n    q = deque([(start, 0)])\n    seen = {start}\n\n    while q:\n        node, dist = q.popleft()\n        if node == target:\n            return dist\n\n        for nei in graph[node]:\n            if nei not in seen:\n                seen.add(nei)\n                q.append((nei, dist + 1))\n\n    return -1\n```\n\nUse BFS for minimum number of edges in unweighted graphs.\n\n## Topological Sort\n\nTopological sort orders nodes in a directed acyclic graph so dependencies come before dependents.\n\nKahn's algorithm:\n\n```python\nfrom collections import deque, defaultdict\n\ndef topo_sort(n, edges):\n    graph = defaultdict(list)\n    indeg = [0] * n\n\n    for a, b in edges:\n        graph[a].append(b)\n        indeg[b] += 1\n\n    q = deque([i for i in range(n) if indeg[i] == 0])\n    order = []\n\n    while q:\n        node = q.popleft()\n        order.append(node)\n\n        for nei in graph[node]:\n            indeg[nei] -= 1\n            if indeg[nei] == 0:\n                q.append(nei)\n\n    return order if len(order) == n else []\n```\n\nIf output length is less than `n`, the graph has a cycle.\n\n## Dijkstra's Algorithm\n\nDijkstra finds shortest paths in graphs with non-negative edge weights.\n\n```python\nimport heapq\nfrom collections import defaultdict\n\ndef dijkstra(n, edges, src):\n    graph = defaultdict(list)\n    for u, v, w in edges:\n        graph[u].append((v, w))\n\n    dist = [float(\"inf\")] * n\n    dist[src] = 0\n\n    heap = [(0, src)]\n\n    while heap:\n        d, node = heapq.heappop(heap)\n\n        if d != dist[node]:\n            continue\n\n        for nei, w in graph[node]:\n            nd = d + w\n            if nd < dist[nei]:\n                dist[nei] = nd\n                heapq.heappush(heap, (nd, nei))\n\n    return dist\n```\n\nDo not use Dijkstra with negative weights. Use Bellman-Ford or another suitable algorithm.\n\n## Minimum Spanning Tree\n\nMST connects all nodes with minimum total edge cost in an undirected weighted graph.\n\nKruskal uses sorting + DSU:\n\n```python\ndef kruskal(n, edges):\n    dsu = DSU(n)\n    total = 0\n    chosen = []\n\n    for w, u, v in sorted(edges):\n        if dsu.union(u, v):\n            total += w\n            chosen.append((u, v, w))\n\n    return total, chosen\n```\n\nUse MST for:\n\n- network cable planning\n- clustering\n- connecting components cheaply\n- minimum-cost infrastructure\n\n## Dynamic Programming\n\nDP is used when problems have:\n\n1. overlapping subproblems\n2. optimal substructure\n\nExample: climbing stairs.\n\n```python\ndef climb_stairs(n):\n    if n <= 2:\n        return n\n\n    prev2, prev1 = 1, 2\n\n    for _ in range(3, n + 1):\n        cur = prev1 + prev2\n        prev2, prev1 = prev1, cur\n\n    return prev1\n```\n\nDP thinking steps:\n\n1. define state\n2. define transition\n3. define base cases\n4. choose order\n5. optimize memory if possible\n\n### 0/1 Knapsack\n\n```python\ndef knapsack(weights, values, capacity):\n    dp = [0] * (capacity + 1)\n\n    for w, v in zip(weights, values):\n        for c in range(capacity, w - 1, -1):\n            dp[c] = max(dp[c], dp[c - w] + v)\n\n    return dp[capacity]\n```\n\nWhy iterate capacity backward?\n\nBecause each item can be used at most once. Forward iteration would allow reusing the same item in the same round.\n\n## Greedy Algorithms\n\nGreedy chooses the locally best option. It works only when local optimal choices lead to global optimum.\n\nInterval scheduling:\n\n```python\ndef max_non_overlapping(intervals):\n    intervals.sort(key=lambda x: x[1])\n\n    count = 0\n    last_end = float(\"-inf\")\n\n    for start, end in intervals:\n        if start >= last_end:\n            count += 1\n            last_end = end\n\n    return count\n```\n\nTo justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.\n\n## Bit Manipulation\n\nUseful operations:\n\n| Expression | Meaning |\n|---|---|\n| `x & 1` | odd/even |\n| `x >> 1` | divide by 2 |\n| `x << 1` | multiply by 2 |\n| `x & (x - 1)` | remove lowest set bit |\n| `x ^ x` | zero |\n| `x ^ 0` | x |\n\nSingle number:\n\n```python\ndef single_number(nums):\n    ans = 0\n    for x in nums:\n        ans ^= x\n    return ans\n```\n\nWorks because duplicates cancel under XOR.\n\n## Algorithm Interview Q&A\n\n**Q: How do you identify DP?**  \nA: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP.\n\n**Q: BFS vs DFS for shortest path?**  \nA: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path.\n\n**Q: Dijkstra vs BFS?**  \nA: BFS works for equal/unweighted edges. Dijkstra works for non-negative weighted edges.\n\n**Q: When should binary search be considered?**  \nA: When input is sorted or the answer predicate is monotonic: false false false true true.\n\n**Q: Why sliding window?**  \nA: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.\n\n",
      "body_markdown": "# Algorithms Interview Notes: Binary Search, Two Pointers, Sliding Window, Sorting, Backtracking, Graphs, DP, Greedy, and Bit Manipulation\n\nAlgorithms are repeatable ways to solve computational problems. These notes focus on pattern recognition, templates, correctness intuition, complexity, and interview-ready explanations.\n\n## How to Approach Algorithm Problems\n\nAlgorithmic thinking starts with classifying the problem.\n\n```mermaid\nflowchart TD\n    P[Problem] --> C{Pattern?}\n    C -->|Sorted / monotonic| BinarySearch\n    C -->|Subarray / substring| SlidingWindow\n    C -->|Pairs / partition| TwoPointers\n    C -->|Choices| Backtracking\n    C -->|Graph reachability| DFS_BFS\n    C -->|Weighted shortest path| Dijkstra\n    C -->|Optimal substructure| DP\n    C -->|Local best works| Greedy\n```\n\nInterview process:\n\n1. clarify input/output\n2. ask constraints\n3. identify brute force\n4. find bottleneck\n5. choose data structure/algorithm\n6. prove correctness informally\n7. analyze complexity\n8. test edge cases\n\n## Binary Search\n\nUse binary search when the answer space is sorted or monotonic.\n\nClassic template:\n\n```python\ndef binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n\n    while lo <= hi:\n        mid = lo + (hi - lo) // 2\n\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n\n    return -1\n```\n\nLower bound:\n\n```python\ndef lower_bound(nums, target):\n    lo, hi = 0, len(nums)\n\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n\n    return lo\n```\n\n**Key:** binary search is not only for arrays. It works on any monotonic predicate.\n\n## Two Pointers\n\nTwo pointers are useful when movement of one or both ends reduces search space.\n\nExample: pair sum in sorted array.\n\n```python\ndef has_pair_sum(nums, target):\n    left, right = 0, len(nums) - 1\n\n    while left < right:\n        s = nums[left] + nums[right]\n        if s == target:\n            return True\n        if s < target:\n            left += 1\n        else:\n            right -= 1\n\n    return False\n```\n\nCommon uses:\n\n- sorted pair problems\n- reversing\n- partitioning\n- removing duplicates\n- palindrome checks\n\n## Sliding Window\n\nSliding window is for contiguous subarray/substring problems.\n\nFixed window:\n\n```python\ndef max_sum_k(nums, k):\n    window = sum(nums[:k])\n    best = window\n\n    for right in range(k, len(nums)):\n        window += nums[right] - nums[right - k]\n        best = max(best, window)\n\n    return best\n```\n\nVariable window:\n\n```python\ndef longest_at_most_k_distinct(s, k):\n    from collections import defaultdict\n\n    freq = defaultdict(int)\n    left = 0\n    best = 0\n\n    for right, ch in enumerate(s):\n        freq[ch] += 1\n\n        while len(freq) > k:\n            old = s[left]\n            freq[old] -= 1\n            if freq[old] == 0:\n                del freq[old]\n            left += 1\n\n        best = max(best, right - left + 1)\n\n    return best\n```\n\n## Sorting Algorithms and When They Matter\n\nCommon sorting complexities:\n\n| Algorithm | Average | Worst | Stable |\n|---|---:|---:|---|\n| Merge sort | O(n log n) | O(n log n) | yes |\n| Quick sort | O(n log n) | O(n²) | usually no |\n| Heap sort | O(n log n) | O(n log n) | no |\n| Counting sort | O(n + k) | O(n + k) | yes possible |\n\nPython's built-in sort is Timsort and is stable.\n\nUse sorting when:\n\n- order reveals structure\n- intervals can be merged\n- greedy choice depends on ordering\n- duplicates grouping becomes easier\n\nMerge intervals:\n\n```python\ndef merge_intervals(intervals):\n    intervals.sort()\n    merged = []\n\n    for start, end in intervals:\n        if not merged or start > merged[-1][1]:\n            merged.append([start, end])\n        else:\n            merged[-1][1] = max(merged[-1][1], end)\n\n    return merged\n```\n\n## Recursion and Backtracking\n\nBacktracking explores choices and undoes them.\n\n```python\ndef subsets(nums):\n    res = []\n    path = []\n\n    def dfs(i):\n        if i == len(nums):\n            res.append(path.copy())\n            return\n\n        path.append(nums[i])\n        dfs(i + 1)\n\n        path.pop()\n        dfs(i + 1)\n\n    dfs(0)\n    return res\n```\n\nBacktracking framework:\n\n```txt\nchoose -> explore -> unchoose\n```\n\nUse for:\n\n- subsets\n- permutations\n- combinations\n- N-Queens\n- Sudoku\n- word search\n\n## BFS and DFS\n\nDFS goes deep; BFS expands level by level.\n\nDFS recursive:\n\n```python\ndef dfs(node, graph, seen):\n    if node in seen:\n        return\n\n    seen.add(node)\n\n    for nei in graph[node]:\n        dfs(nei, graph, seen)\n```\n\nBFS shortest path in unweighted graph:\n\n```python\nfrom collections import deque\n\ndef shortest_path_unweighted(graph, start, target):\n    q = deque([(start, 0)])\n    seen = {start}\n\n    while q:\n        node, dist = q.popleft()\n        if node == target:\n            return dist\n\n        for nei in graph[node]:\n            if nei not in seen:\n                seen.add(nei)\n                q.append((nei, dist + 1))\n\n    return -1\n```\n\nUse BFS for minimum number of edges in unweighted graphs.\n\n## Topological Sort\n\nTopological sort orders nodes in a directed acyclic graph so dependencies come before dependents.\n\nKahn's algorithm:\n\n```python\nfrom collections import deque, defaultdict\n\ndef topo_sort(n, edges):\n    graph = defaultdict(list)\n    indeg = [0] * n\n\n    for a, b in edges:\n        graph[a].append(b)\n        indeg[b] += 1\n\n    q = deque([i for i in range(n) if indeg[i] == 0])\n    order = []\n\n    while q:\n        node = q.popleft()\n        order.append(node)\n\n        for nei in graph[node]:\n            indeg[nei] -= 1\n            if indeg[nei] == 0:\n                q.append(nei)\n\n    return order if len(order) == n else []\n```\n\nIf output length is less than `n`, the graph has a cycle.\n\n## Dijkstra's Algorithm\n\nDijkstra finds shortest paths in graphs with non-negative edge weights.\n\n```python\nimport heapq\nfrom collections import defaultdict\n\ndef dijkstra(n, edges, src):\n    graph = defaultdict(list)\n    for u, v, w in edges:\n        graph[u].append((v, w))\n\n    dist = [float(\"inf\")] * n\n    dist[src] = 0\n\n    heap = [(0, src)]\n\n    while heap:\n        d, node = heapq.heappop(heap)\n\n        if d != dist[node]:\n            continue\n\n        for nei, w in graph[node]:\n            nd = d + w\n            if nd < dist[nei]:\n                dist[nei] = nd\n                heapq.heappush(heap, (nd, nei))\n\n    return dist\n```\n\nDo not use Dijkstra with negative weights. Use Bellman-Ford or another suitable algorithm.\n\n## Minimum Spanning Tree\n\nMST connects all nodes with minimum total edge cost in an undirected weighted graph.\n\nKruskal uses sorting + DSU:\n\n```python\ndef kruskal(n, edges):\n    dsu = DSU(n)\n    total = 0\n    chosen = []\n\n    for w, u, v in sorted(edges):\n        if dsu.union(u, v):\n            total += w\n            chosen.append((u, v, w))\n\n    return total, chosen\n```\n\nUse MST for:\n\n- network cable planning\n- clustering\n- connecting components cheaply\n- minimum-cost infrastructure\n\n## Dynamic Programming\n\nDP is used when problems have:\n\n1. overlapping subproblems\n2. optimal substructure\n\nExample: climbing stairs.\n\n```python\ndef climb_stairs(n):\n    if n <= 2:\n        return n\n\n    prev2, prev1 = 1, 2\n\n    for _ in range(3, n + 1):\n        cur = prev1 + prev2\n        prev2, prev1 = prev1, cur\n\n    return prev1\n```\n\nDP thinking steps:\n\n1. define state\n2. define transition\n3. define base cases\n4. choose order\n5. optimize memory if possible\n\n### 0/1 Knapsack\n\n```python\ndef knapsack(weights, values, capacity):\n    dp = [0] * (capacity + 1)\n\n    for w, v in zip(weights, values):\n        for c in range(capacity, w - 1, -1):\n            dp[c] = max(dp[c], dp[c - w] + v)\n\n    return dp[capacity]\n```\n\nWhy iterate capacity backward?\n\nBecause each item can be used at most once. Forward iteration would allow reusing the same item in the same round.\n\n## Greedy Algorithms\n\nGreedy chooses the locally best option. It works only when local optimal choices lead to global optimum.\n\nInterval scheduling:\n\n```python\ndef max_non_overlapping(intervals):\n    intervals.sort(key=lambda x: x[1])\n\n    count = 0\n    last_end = float(\"-inf\")\n\n    for start, end in intervals:\n        if start >= last_end:\n            count += 1\n            last_end = end\n\n    return count\n```\n\nTo justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.\n\n## Bit Manipulation\n\nUseful operations:\n\n| Expression | Meaning |\n|---|---|\n| `x & 1` | odd/even |\n| `x >> 1` | divide by 2 |\n| `x << 1` | multiply by 2 |\n| `x & (x - 1)` | remove lowest set bit |\n| `x ^ x` | zero |\n| `x ^ 0` | x |\n\nSingle number:\n\n```python\ndef single_number(nums):\n    ans = 0\n    for x in nums:\n        ans ^= x\n    return ans\n```\n\nWorks because duplicates cancel under XOR.\n\n## Algorithm Interview Q&A\n\n**Q: How do you identify DP?**  \nA: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP.\n\n**Q: BFS vs DFS for shortest path?**  \nA: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path.\n\n**Q: Dijkstra vs BFS?**  \nA: BFS works for equal/unweighted edges. Dijkstra works for non-negative weighted edges.\n\n**Q: When should binary search be considered?**  \nA: When input is sorted or the answer predicate is monotonic: false false false true true.\n\n**Q: Why sliding window?**  \nA: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.\n\n",
      "sections": [
        {
          "id": "algorithms-interview-deep-dive__how-to-approach-algorithm-problems",
          "slug": "how-to-approach-algorithm-problems",
          "anchor": "algorithms-interview-deep-dive__how-to-approach-algorithm-problems",
          "title": "How to Approach Algorithm Problems",
          "level": 1,
          "order_index": 1,
          "parent_id": null,
          "content": "Algorithmic thinking starts with classifying the problem.\n\n```mermaid\nflowchart TD\n    P[Problem] --> C{Pattern?}\n    C -->|Sorted / monotonic| BinarySearch\n    C -->|Subarray / substring| SlidingWindow\n    C -->|Pairs / partition| TwoPointers\n    C -->|Choices| Backtracking\n    C -->|Graph reachability| DFS_BFS\n    C -->|Weighted shortest path| Dijkstra\n    C -->|Optimal substructure| DP\n    C -->|Local best works| Greedy\n```\n\nInterview process:\n\n1. clarify input/output\n2. ask constraints\n3. identify brute force\n4. find bottleneck\n5. choose data structure/algorithm\n6. prove correctness informally\n7. analyze complexity\n8. test edge cases\n",
          "body_markdown": "Algorithmic thinking starts with classifying the problem.\n\n```mermaid\nflowchart TD\n    P[Problem] --> C{Pattern?}\n    C -->|Sorted / monotonic| BinarySearch\n    C -->|Subarray / substring| SlidingWindow\n    C -->|Pairs / partition| TwoPointers\n    C -->|Choices| Backtracking\n    C -->|Graph reachability| DFS_BFS\n    C -->|Weighted shortest path| Dijkstra\n    C -->|Optimal substructure| DP\n    C -->|Local best works| Greedy\n```\n\nInterview process:\n\n1. clarify input/output\n2. ask constraints\n3. identify brute force\n4. find bottleneck\n5. choose data structure/algorithm\n6. prove correctness informally\n7. analyze complexity\n8. test edge cases\n",
          "rawText": "Algorithmic thinking starts with classifying the problem. code block Interview process: 1. clarify input/output 2. ask constraints 3. identify brute force 4. find bottleneck 5. choose data structure/algorithm 6. prove correctness informally 7. analyze complexity 8. test edge cases",
          "raw_text": "Algorithmic thinking starts with classifying the problem. code block Interview process: 1. clarify input/output 2. ask constraints 3. identify brute force 4. find bottleneck 5. choose data structure/algorithm 6. prove correctness informally 7. analyze complexity 8. test edge cases",
          "plainText": "Algorithmic thinking starts with classifying the problem. code block Interview process: 1. clarify input/output 2. ask constraints 3. identify brute force 4. find bottleneck 5. choose data structure/algorithm 6. prove correctness informally 7. analyze complexity 8. test edge cases",
          "plain_text": "Algorithmic thinking starts with classifying the problem. code block Interview process: 1. clarify input/output 2. ask constraints 3. identify brute force 4. find bottleneck 5. choose data structure/algorithm 6. prove correctness informally 7. analyze complexity 8. test edge cases",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__binary-search",
          "slug": "binary-search",
          "anchor": "algorithms-interview-deep-dive__binary-search",
          "title": "Binary Search",
          "level": 1,
          "order_index": 2,
          "parent_id": null,
          "content": "Use binary search when the answer space is sorted or monotonic.\n\nClassic template:\n\n```python\ndef binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n\n    while lo <= hi:\n        mid = lo + (hi - lo) // 2\n\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n\n    return -1\n```\n\nLower bound:\n\n```python\ndef lower_bound(nums, target):\n    lo, hi = 0, len(nums)\n\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n\n    return lo\n```\n\n**Key:** binary search is not only for arrays. It works on any monotonic predicate.\n",
          "body_markdown": "Use binary search when the answer space is sorted or monotonic.\n\nClassic template:\n\n```python\ndef binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n\n    while lo <= hi:\n        mid = lo + (hi - lo) // 2\n\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n\n    return -1\n```\n\nLower bound:\n\n```python\ndef lower_bound(nums, target):\n    lo, hi = 0, len(nums)\n\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid\n\n    return lo\n```\n\n**Key:** binary search is not only for arrays. It works on any monotonic predicate.\n",
          "rawText": "Use binary search when the answer space is sorted or monotonic. Classic template: code block Lower bound: code block Key: binary search is not only for arrays. It works on any monotonic predicate.",
          "raw_text": "Use binary search when the answer space is sorted or monotonic. Classic template: code block Lower bound: code block Key: binary search is not only for arrays. It works on any monotonic predicate.",
          "plainText": "Use binary search when the answer space is sorted or monotonic. Classic template: code block Lower bound: code block Key: binary search is not only for arrays. It works on any monotonic predicate.",
          "plain_text": "Use binary search when the answer space is sorted or monotonic. Classic template: code block Lower bound: code block Key: binary search is not only for arrays. It works on any monotonic predicate.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__two-pointers",
          "slug": "two-pointers",
          "anchor": "algorithms-interview-deep-dive__two-pointers",
          "title": "Two Pointers",
          "level": 1,
          "order_index": 3,
          "parent_id": null,
          "content": "Two pointers are useful when movement of one or both ends reduces search space.\n\nExample: pair sum in sorted array.\n\n```python\ndef has_pair_sum(nums, target):\n    left, right = 0, len(nums) - 1\n\n    while left < right:\n        s = nums[left] + nums[right]\n        if s == target:\n            return True\n        if s < target:\n            left += 1\n        else:\n            right -= 1\n\n    return False\n```\n\nCommon uses:\n\n- sorted pair problems\n- reversing\n- partitioning\n- removing duplicates\n- palindrome checks\n",
          "body_markdown": "Two pointers are useful when movement of one or both ends reduces search space.\n\nExample: pair sum in sorted array.\n\n```python\ndef has_pair_sum(nums, target):\n    left, right = 0, len(nums) - 1\n\n    while left < right:\n        s = nums[left] + nums[right]\n        if s == target:\n            return True\n        if s < target:\n            left += 1\n        else:\n            right -= 1\n\n    return False\n```\n\nCommon uses:\n\n- sorted pair problems\n- reversing\n- partitioning\n- removing duplicates\n- palindrome checks\n",
          "rawText": "Two pointers are useful when movement of one or both ends reduces search space. Example: pair sum in sorted array. code block Common uses: sorted pair problems reversing partitioning removing duplicates palindrome checks",
          "raw_text": "Two pointers are useful when movement of one or both ends reduces search space. Example: pair sum in sorted array. code block Common uses: sorted pair problems reversing partitioning removing duplicates palindrome checks",
          "plainText": "Two pointers are useful when movement of one or both ends reduces search space. Example: pair sum in sorted array. code block Common uses: sorted pair problems reversing partitioning removing duplicates palindrome checks",
          "plain_text": "Two pointers are useful when movement of one or both ends reduces search space. Example: pair sum in sorted array. code block Common uses: sorted pair problems reversing partitioning removing duplicates palindrome checks",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__sliding-window",
          "slug": "sliding-window",
          "anchor": "algorithms-interview-deep-dive__sliding-window",
          "title": "Sliding Window",
          "level": 1,
          "order_index": 4,
          "parent_id": null,
          "content": "Sliding window is for contiguous subarray/substring problems.\n\nFixed window:\n\n```python\ndef max_sum_k(nums, k):\n    window = sum(nums[:k])\n    best = window\n\n    for right in range(k, len(nums)):\n        window += nums[right] - nums[right - k]\n        best = max(best, window)\n\n    return best\n```\n\nVariable window:\n\n```python\ndef longest_at_most_k_distinct(s, k):\n    from collections import defaultdict\n\n    freq = defaultdict(int)\n    left = 0\n    best = 0\n\n    for right, ch in enumerate(s):\n        freq[ch] += 1\n\n        while len(freq) > k:\n            old = s[left]\n            freq[old] -= 1\n            if freq[old] == 0:\n                del freq[old]\n            left += 1\n\n        best = max(best, right - left + 1)\n\n    return best\n```\n",
          "body_markdown": "Sliding window is for contiguous subarray/substring problems.\n\nFixed window:\n\n```python\ndef max_sum_k(nums, k):\n    window = sum(nums[:k])\n    best = window\n\n    for right in range(k, len(nums)):\n        window += nums[right] - nums[right - k]\n        best = max(best, window)\n\n    return best\n```\n\nVariable window:\n\n```python\ndef longest_at_most_k_distinct(s, k):\n    from collections import defaultdict\n\n    freq = defaultdict(int)\n    left = 0\n    best = 0\n\n    for right, ch in enumerate(s):\n        freq[ch] += 1\n\n        while len(freq) > k:\n            old = s[left]\n            freq[old] -= 1\n            if freq[old] == 0:\n                del freq[old]\n            left += 1\n\n        best = max(best, right - left + 1)\n\n    return best\n```\n",
          "rawText": "Sliding window is for contiguous subarray/substring problems. Fixed window: code block Variable window: code block",
          "raw_text": "Sliding window is for contiguous subarray/substring problems. Fixed window: code block Variable window: code block",
          "plainText": "Sliding window is for contiguous subarray/substring problems. Fixed window: code block Variable window: code block",
          "plain_text": "Sliding window is for contiguous subarray/substring problems. Fixed window: code block Variable window: code block",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__sorting-algorithms-and-when-they-matter",
          "slug": "sorting-algorithms-and-when-they-matter",
          "anchor": "algorithms-interview-deep-dive__sorting-algorithms-and-when-they-matter",
          "title": "Sorting Algorithms and When They Matter",
          "level": 1,
          "order_index": 5,
          "parent_id": null,
          "content": "Common sorting complexities:\n\n| Algorithm | Average | Worst | Stable |\n|---|---:|---:|---|\n| Merge sort | O(n log n) | O(n log n) | yes |\n| Quick sort | O(n log n) | O(n²) | usually no |\n| Heap sort | O(n log n) | O(n log n) | no |\n| Counting sort | O(n + k) | O(n + k) | yes possible |\n\nPython's built-in sort is Timsort and is stable.\n\nUse sorting when:\n\n- order reveals structure\n- intervals can be merged\n- greedy choice depends on ordering\n- duplicates grouping becomes easier\n\nMerge intervals:\n\n```python\ndef merge_intervals(intervals):\n    intervals.sort()\n    merged = []\n\n    for start, end in intervals:\n        if not merged or start > merged[-1][1]:\n            merged.append([start, end])\n        else:\n            merged[-1][1] = max(merged[-1][1], end)\n\n    return merged\n```\n",
          "body_markdown": "Common sorting complexities:\n\n| Algorithm | Average | Worst | Stable |\n|---|---:|---:|---|\n| Merge sort | O(n log n) | O(n log n) | yes |\n| Quick sort | O(n log n) | O(n²) | usually no |\n| Heap sort | O(n log n) | O(n log n) | no |\n| Counting sort | O(n + k) | O(n + k) | yes possible |\n\nPython's built-in sort is Timsort and is stable.\n\nUse sorting when:\n\n- order reveals structure\n- intervals can be merged\n- greedy choice depends on ordering\n- duplicates grouping becomes easier\n\nMerge intervals:\n\n```python\ndef merge_intervals(intervals):\n    intervals.sort()\n    merged = []\n\n    for start, end in intervals:\n        if not merged or start > merged[-1][1]:\n            merged.append([start, end])\n        else:\n            merged[-1][1] = max(merged[-1][1], end)\n\n    return merged\n```\n",
          "rawText": "Common sorting complexities: Algorithm Average Worst Stable : : Merge sort O(n log n) O(n log n) yes Quick sort O(n log n) O(n²) usually no Heap sort O(n log n) O(n log n) no Counting sort O(n + k) O(n + k) yes possible Python's built in sort is Timsort and is stable. Use sorting when: order reveals structure intervals can be merged greedy choice depends on ordering duplicates grouping becomes easier Merge intervals: code block",
          "raw_text": "Common sorting complexities: Algorithm Average Worst Stable : : Merge sort O(n log n) O(n log n) yes Quick sort O(n log n) O(n²) usually no Heap sort O(n log n) O(n log n) no Counting sort O(n + k) O(n + k) yes possible Python's built in sort is Timsort and is stable. Use sorting when: order reveals structure intervals can be merged greedy choice depends on ordering duplicates grouping becomes easier Merge intervals: code block",
          "plainText": "Common sorting complexities: Algorithm Average Worst Stable : : Merge sort O(n log n) O(n log n) yes Quick sort O(n log n) O(n²) usually no Heap sort O(n log n) O(n log n) no Counting sort O(n + k) O(n + k) yes possible Python's built in sort is Timsort and is stable. Use sorting when: order reveals structure intervals can be merged greedy choice depends on ordering duplicates grouping becomes easier Merge intervals: code block",
          "plain_text": "Common sorting complexities: Algorithm Average Worst Stable : : Merge sort O(n log n) O(n log n) yes Quick sort O(n log n) O(n²) usually no Heap sort O(n log n) O(n log n) no Counting sort O(n + k) O(n + k) yes possible Python's built in sort is Timsort and is stable. Use sorting when: order reveals structure intervals can be merged greedy choice depends on ordering duplicates grouping becomes easier Merge intervals: code block",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__recursion-and-backtracking",
          "slug": "recursion-and-backtracking",
          "anchor": "algorithms-interview-deep-dive__recursion-and-backtracking",
          "title": "Recursion and Backtracking",
          "level": 1,
          "order_index": 6,
          "parent_id": null,
          "content": "Backtracking explores choices and undoes them.\n\n```python\ndef subsets(nums):\n    res = []\n    path = []\n\n    def dfs(i):\n        if i == len(nums):\n            res.append(path.copy())\n            return\n\n        path.append(nums[i])\n        dfs(i + 1)\n\n        path.pop()\n        dfs(i + 1)\n\n    dfs(0)\n    return res\n```\n\nBacktracking framework:\n\n```txt\nchoose -> explore -> unchoose\n```\n\nUse for:\n\n- subsets\n- permutations\n- combinations\n- N-Queens\n- Sudoku\n- word search\n",
          "body_markdown": "Backtracking explores choices and undoes them.\n\n```python\ndef subsets(nums):\n    res = []\n    path = []\n\n    def dfs(i):\n        if i == len(nums):\n            res.append(path.copy())\n            return\n\n        path.append(nums[i])\n        dfs(i + 1)\n\n        path.pop()\n        dfs(i + 1)\n\n    dfs(0)\n    return res\n```\n\nBacktracking framework:\n\n```txt\nchoose -> explore -> unchoose\n```\n\nUse for:\n\n- subsets\n- permutations\n- combinations\n- N-Queens\n- Sudoku\n- word search\n",
          "rawText": "Backtracking explores choices and undoes them. code block Backtracking framework: code block Use for: subsets permutations combinations N Queens Sudoku word search",
          "raw_text": "Backtracking explores choices and undoes them. code block Backtracking framework: code block Use for: subsets permutations combinations N Queens Sudoku word search",
          "plainText": "Backtracking explores choices and undoes them. code block Backtracking framework: code block Use for: subsets permutations combinations N Queens Sudoku word search",
          "plain_text": "Backtracking explores choices and undoes them. code block Backtracking framework: code block Use for: subsets permutations combinations N Queens Sudoku word search",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__bfs-and-dfs",
          "slug": "bfs-and-dfs",
          "anchor": "algorithms-interview-deep-dive__bfs-and-dfs",
          "title": "BFS and DFS",
          "level": 1,
          "order_index": 7,
          "parent_id": null,
          "content": "DFS goes deep; BFS expands level by level.\n\nDFS recursive:\n\n```python\ndef dfs(node, graph, seen):\n    if node in seen:\n        return\n\n    seen.add(node)\n\n    for nei in graph[node]:\n        dfs(nei, graph, seen)\n```\n\nBFS shortest path in unweighted graph:\n\n```python\nfrom collections import deque\n\ndef shortest_path_unweighted(graph, start, target):\n    q = deque([(start, 0)])\n    seen = {start}\n\n    while q:\n        node, dist = q.popleft()\n        if node == target:\n            return dist\n\n        for nei in graph[node]:\n            if nei not in seen:\n                seen.add(nei)\n                q.append((nei, dist + 1))\n\n    return -1\n```\n\nUse BFS for minimum number of edges in unweighted graphs.\n",
          "body_markdown": "DFS goes deep; BFS expands level by level.\n\nDFS recursive:\n\n```python\ndef dfs(node, graph, seen):\n    if node in seen:\n        return\n\n    seen.add(node)\n\n    for nei in graph[node]:\n        dfs(nei, graph, seen)\n```\n\nBFS shortest path in unweighted graph:\n\n```python\nfrom collections import deque\n\ndef shortest_path_unweighted(graph, start, target):\n    q = deque([(start, 0)])\n    seen = {start}\n\n    while q:\n        node, dist = q.popleft()\n        if node == target:\n            return dist\n\n        for nei in graph[node]:\n            if nei not in seen:\n                seen.add(nei)\n                q.append((nei, dist + 1))\n\n    return -1\n```\n\nUse BFS for minimum number of edges in unweighted graphs.\n",
          "rawText": "DFS goes deep; BFS expands level by level. DFS recursive: code block BFS shortest path in unweighted graph: code block Use BFS for minimum number of edges in unweighted graphs.",
          "raw_text": "DFS goes deep; BFS expands level by level. DFS recursive: code block BFS shortest path in unweighted graph: code block Use BFS for minimum number of edges in unweighted graphs.",
          "plainText": "DFS goes deep; BFS expands level by level. DFS recursive: code block BFS shortest path in unweighted graph: code block Use BFS for minimum number of edges in unweighted graphs.",
          "plain_text": "DFS goes deep; BFS expands level by level. DFS recursive: code block BFS shortest path in unweighted graph: code block Use BFS for minimum number of edges in unweighted graphs.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__topological-sort",
          "slug": "topological-sort",
          "anchor": "algorithms-interview-deep-dive__topological-sort",
          "title": "Topological Sort",
          "level": 1,
          "order_index": 8,
          "parent_id": null,
          "content": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents.\n\nKahn's algorithm:\n\n```python\nfrom collections import deque, defaultdict\n\ndef topo_sort(n, edges):\n    graph = defaultdict(list)\n    indeg = [0] * n\n\n    for a, b in edges:\n        graph[a].append(b)\n        indeg[b] += 1\n\n    q = deque([i for i in range(n) if indeg[i] == 0])\n    order = []\n\n    while q:\n        node = q.popleft()\n        order.append(node)\n\n        for nei in graph[node]:\n            indeg[nei] -= 1\n            if indeg[nei] == 0:\n                q.append(nei)\n\n    return order if len(order) == n else []\n```\n\nIf output length is less than `n`, the graph has a cycle.\n",
          "body_markdown": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents.\n\nKahn's algorithm:\n\n```python\nfrom collections import deque, defaultdict\n\ndef topo_sort(n, edges):\n    graph = defaultdict(list)\n    indeg = [0] * n\n\n    for a, b in edges:\n        graph[a].append(b)\n        indeg[b] += 1\n\n    q = deque([i for i in range(n) if indeg[i] == 0])\n    order = []\n\n    while q:\n        node = q.popleft()\n        order.append(node)\n\n        for nei in graph[node]:\n            indeg[nei] -= 1\n            if indeg[nei] == 0:\n                q.append(nei)\n\n    return order if len(order) == n else []\n```\n\nIf output length is less than `n`, the graph has a cycle.\n",
          "rawText": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents. Kahn's algorithm: code block If output length is less than n, the graph has a cycle.",
          "raw_text": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents. Kahn's algorithm: code block If output length is less than n, the graph has a cycle.",
          "plainText": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents. Kahn's algorithm: code block If output length is less than n, the graph has a cycle.",
          "plain_text": "Topological sort orders nodes in a directed acyclic graph so dependencies come before dependents. Kahn's algorithm: code block If output length is less than n, the graph has a cycle.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__dijkstra-s-algorithm",
          "slug": "dijkstra-s-algorithm",
          "anchor": "algorithms-interview-deep-dive__dijkstra-s-algorithm",
          "title": "Dijkstra's Algorithm",
          "level": 1,
          "order_index": 9,
          "parent_id": null,
          "content": "Dijkstra finds shortest paths in graphs with non-negative edge weights.\n\n```python\nimport heapq\nfrom collections import defaultdict\n\ndef dijkstra(n, edges, src):\n    graph = defaultdict(list)\n    for u, v, w in edges:\n        graph[u].append((v, w))\n\n    dist = [float(\"inf\")] * n\n    dist[src] = 0\n\n    heap = [(0, src)]\n\n    while heap:\n        d, node = heapq.heappop(heap)\n\n        if d != dist[node]:\n            continue\n\n        for nei, w in graph[node]:\n            nd = d + w\n            if nd < dist[nei]:\n                dist[nei] = nd\n                heapq.heappush(heap, (nd, nei))\n\n    return dist\n```\n\nDo not use Dijkstra with negative weights. Use Bellman-Ford or another suitable algorithm.\n",
          "body_markdown": "Dijkstra finds shortest paths in graphs with non-negative edge weights.\n\n```python\nimport heapq\nfrom collections import defaultdict\n\ndef dijkstra(n, edges, src):\n    graph = defaultdict(list)\n    for u, v, w in edges:\n        graph[u].append((v, w))\n\n    dist = [float(\"inf\")] * n\n    dist[src] = 0\n\n    heap = [(0, src)]\n\n    while heap:\n        d, node = heapq.heappop(heap)\n\n        if d != dist[node]:\n            continue\n\n        for nei, w in graph[node]:\n            nd = d + w\n            if nd < dist[nei]:\n                dist[nei] = nd\n                heapq.heappush(heap, (nd, nei))\n\n    return dist\n```\n\nDo not use Dijkstra with negative weights. Use Bellman-Ford or another suitable algorithm.\n",
          "rawText": "Dijkstra finds shortest paths in graphs with non negative edge weights. code block Do not use Dijkstra with negative weights. Use Bellman Ford or another suitable algorithm.",
          "raw_text": "Dijkstra finds shortest paths in graphs with non negative edge weights. code block Do not use Dijkstra with negative weights. Use Bellman Ford or another suitable algorithm.",
          "plainText": "Dijkstra finds shortest paths in graphs with non negative edge weights. code block Do not use Dijkstra with negative weights. Use Bellman Ford or another suitable algorithm.",
          "plain_text": "Dijkstra finds shortest paths in graphs with non negative edge weights. code block Do not use Dijkstra with negative weights. Use Bellman Ford or another suitable algorithm.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__minimum-spanning-tree",
          "slug": "minimum-spanning-tree",
          "anchor": "algorithms-interview-deep-dive__minimum-spanning-tree",
          "title": "Minimum Spanning Tree",
          "level": 1,
          "order_index": 10,
          "parent_id": null,
          "content": "MST connects all nodes with minimum total edge cost in an undirected weighted graph.\n\nKruskal uses sorting + DSU:\n\n```python\ndef kruskal(n, edges):\n    dsu = DSU(n)\n    total = 0\n    chosen = []\n\n    for w, u, v in sorted(edges):\n        if dsu.union(u, v):\n            total += w\n            chosen.append((u, v, w))\n\n    return total, chosen\n```\n\nUse MST for:\n\n- network cable planning\n- clustering\n- connecting components cheaply\n- minimum-cost infrastructure\n",
          "body_markdown": "MST connects all nodes with minimum total edge cost in an undirected weighted graph.\n\nKruskal uses sorting + DSU:\n\n```python\ndef kruskal(n, edges):\n    dsu = DSU(n)\n    total = 0\n    chosen = []\n\n    for w, u, v in sorted(edges):\n        if dsu.union(u, v):\n            total += w\n            chosen.append((u, v, w))\n\n    return total, chosen\n```\n\nUse MST for:\n\n- network cable planning\n- clustering\n- connecting components cheaply\n- minimum-cost infrastructure\n",
          "rawText": "MST connects all nodes with minimum total edge cost in an undirected weighted graph. Kruskal uses sorting + DSU: code block Use MST for: network cable planning clustering connecting components cheaply minimum cost infrastructure",
          "raw_text": "MST connects all nodes with minimum total edge cost in an undirected weighted graph. Kruskal uses sorting + DSU: code block Use MST for: network cable planning clustering connecting components cheaply minimum cost infrastructure",
          "plainText": "MST connects all nodes with minimum total edge cost in an undirected weighted graph. Kruskal uses sorting + DSU: code block Use MST for: network cable planning clustering connecting components cheaply minimum cost infrastructure",
          "plain_text": "MST connects all nodes with minimum total edge cost in an undirected weighted graph. Kruskal uses sorting + DSU: code block Use MST for: network cable planning clustering connecting components cheaply minimum cost infrastructure",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__dynamic-programming",
          "slug": "dynamic-programming",
          "anchor": "algorithms-interview-deep-dive__dynamic-programming",
          "title": "Dynamic Programming",
          "level": 1,
          "order_index": 11,
          "parent_id": null,
          "content": "DP is used when problems have:\n\n1. overlapping subproblems\n2. optimal substructure\n\nExample: climbing stairs.\n\n```python\ndef climb_stairs(n):\n    if n <= 2:\n        return n\n\n    prev2, prev1 = 1, 2\n\n    for _ in range(3, n + 1):\n        cur = prev1 + prev2\n        prev2, prev1 = prev1, cur\n\n    return prev1\n```\n\nDP thinking steps:\n\n1. define state\n2. define transition\n3. define base cases\n4. choose order\n5. optimize memory if possible\n",
          "body_markdown": "DP is used when problems have:\n\n1. overlapping subproblems\n2. optimal substructure\n\nExample: climbing stairs.\n\n```python\ndef climb_stairs(n):\n    if n <= 2:\n        return n\n\n    prev2, prev1 = 1, 2\n\n    for _ in range(3, n + 1):\n        cur = prev1 + prev2\n        prev2, prev1 = prev1, cur\n\n    return prev1\n```\n\nDP thinking steps:\n\n1. define state\n2. define transition\n3. define base cases\n4. choose order\n5. optimize memory if possible\n",
          "rawText": "DP is used when problems have: 1. overlapping subproblems 2. optimal substructure Example: climbing stairs. code block DP thinking steps: 1. define state 2. define transition 3. define base cases 4. choose order 5. optimize memory if possible",
          "raw_text": "DP is used when problems have: 1. overlapping subproblems 2. optimal substructure Example: climbing stairs. code block DP thinking steps: 1. define state 2. define transition 3. define base cases 4. choose order 5. optimize memory if possible",
          "plainText": "DP is used when problems have: 1. overlapping subproblems 2. optimal substructure Example: climbing stairs. code block DP thinking steps: 1. define state 2. define transition 3. define base cases 4. choose order 5. optimize memory if possible",
          "plain_text": "DP is used when problems have: 1. overlapping subproblems 2. optimal substructure Example: climbing stairs. code block DP thinking steps: 1. define state 2. define transition 3. define base cases 4. choose order 5. optimize memory if possible",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__0-1-knapsack",
          "slug": "0-1-knapsack",
          "anchor": "algorithms-interview-deep-dive__0-1-knapsack",
          "title": "0/1 Knapsack",
          "level": 2,
          "order_index": 12,
          "parent_id": "algorithms-interview-deep-dive__dynamic-programming",
          "content": "```python\ndef knapsack(weights, values, capacity):\n    dp = [0] * (capacity + 1)\n\n    for w, v in zip(weights, values):\n        for c in range(capacity, w - 1, -1):\n            dp[c] = max(dp[c], dp[c - w] + v)\n\n    return dp[capacity]\n```\n\nWhy iterate capacity backward?\n\nBecause each item can be used at most once. Forward iteration would allow reusing the same item in the same round.\n",
          "body_markdown": "```python\ndef knapsack(weights, values, capacity):\n    dp = [0] * (capacity + 1)\n\n    for w, v in zip(weights, values):\n        for c in range(capacity, w - 1, -1):\n            dp[c] = max(dp[c], dp[c - w] + v)\n\n    return dp[capacity]\n```\n\nWhy iterate capacity backward?\n\nBecause each item can be used at most once. Forward iteration would allow reusing the same item in the same round.\n",
          "rawText": "code block Why iterate capacity backward? Because each item can be used at most once. Forward iteration would allow reusing the same item in the same round.",
          "raw_text": "code block Why iterate capacity backward? Because each item can be used at most once. Forward iteration would allow reusing the same item in the same round.",
          "plainText": "code block Why iterate capacity backward? Because each item can be used at most once. Forward iteration would allow reusing the same item in the same round.",
          "plain_text": "code block Why iterate capacity backward? Because each item can be used at most once. Forward iteration would allow reusing the same item in the same round.",
          "parentId": "algorithms-interview-deep-dive__dynamic-programming",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__greedy-algorithms",
          "slug": "greedy-algorithms",
          "anchor": "algorithms-interview-deep-dive__greedy-algorithms",
          "title": "Greedy Algorithms",
          "level": 1,
          "order_index": 13,
          "parent_id": null,
          "content": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum.\n\nInterval scheduling:\n\n```python\ndef max_non_overlapping(intervals):\n    intervals.sort(key=lambda x: x[1])\n\n    count = 0\n    last_end = float(\"-inf\")\n\n    for start, end in intervals:\n        if start >= last_end:\n            count += 1\n            last_end = end\n\n    return count\n```\n\nTo justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.\n",
          "body_markdown": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum.\n\nInterval scheduling:\n\n```python\ndef max_non_overlapping(intervals):\n    intervals.sort(key=lambda x: x[1])\n\n    count = 0\n    last_end = float(\"-inf\")\n\n    for start, end in intervals:\n        if start >= last_end:\n            count += 1\n            last_end = end\n\n    return count\n```\n\nTo justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.\n",
          "rawText": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum. Interval scheduling: code block To justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.",
          "raw_text": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum. Interval scheduling: code block To justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.",
          "plainText": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum. Interval scheduling: code block To justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.",
          "plain_text": "Greedy chooses the locally best option. It works only when local optimal choices lead to global optimum. Interval scheduling: code block To justify greedy, explain why choosing earliest finishing interval leaves maximum room for future intervals.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__bit-manipulation",
          "slug": "bit-manipulation",
          "anchor": "algorithms-interview-deep-dive__bit-manipulation",
          "title": "Bit Manipulation",
          "level": 1,
          "order_index": 14,
          "parent_id": null,
          "content": "Useful operations:\n\n| Expression | Meaning |\n|---|---|\n| `x & 1` | odd/even |\n| `x >> 1` | divide by 2 |\n| `x << 1` | multiply by 2 |\n| `x & (x - 1)` | remove lowest set bit |\n| `x ^ x` | zero |\n| `x ^ 0` | x |\n\nSingle number:\n\n```python\ndef single_number(nums):\n    ans = 0\n    for x in nums:\n        ans ^= x\n    return ans\n```\n\nWorks because duplicates cancel under XOR.\n",
          "body_markdown": "Useful operations:\n\n| Expression | Meaning |\n|---|---|\n| `x & 1` | odd/even |\n| `x >> 1` | divide by 2 |\n| `x << 1` | multiply by 2 |\n| `x & (x - 1)` | remove lowest set bit |\n| `x ^ x` | zero |\n| `x ^ 0` | x |\n\nSingle number:\n\n```python\ndef single_number(nums):\n    ans = 0\n    for x in nums:\n        ans ^= x\n    return ans\n```\n\nWorks because duplicates cancel under XOR.\n",
          "rawText": "Useful operations: Expression Meaning x & 1 odd/even x 1 divide by 2 x << 1 multiply by 2 x & (x 1) remove lowest set bit x ^ x zero x ^ 0 x Single number: code block Works because duplicates cancel under XOR.",
          "raw_text": "Useful operations: Expression Meaning x & 1 odd/even x 1 divide by 2 x << 1 multiply by 2 x & (x 1) remove lowest set bit x ^ x zero x ^ 0 x Single number: code block Works because duplicates cancel under XOR.",
          "plainText": "Useful operations: Expression Meaning x & 1 odd/even x 1 divide by 2 x << 1 multiply by 2 x & (x 1) remove lowest set bit x ^ x zero x ^ 0 x Single number: code block Works because duplicates cancel under XOR.",
          "plain_text": "Useful operations: Expression Meaning x & 1 odd/even x 1 divide by 2 x << 1 multiply by 2 x & (x 1) remove lowest set bit x ^ x zero x ^ 0 x Single number: code block Works because duplicates cancel under XOR.",
          "kind": "section"
        },
        {
          "id": "algorithms-interview-deep-dive__algorithm-interview-q-a",
          "slug": "algorithm-interview-q-a",
          "anchor": "algorithms-interview-deep-dive__algorithm-interview-q-a",
          "title": "Algorithm Interview Q&A",
          "level": 1,
          "order_index": 15,
          "parent_id": null,
          "content": "**Q: How do you identify DP?**  \nA: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP.\n\n**Q: BFS vs DFS for shortest path?**  \nA: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path.\n\n**Q: Dijkstra vs BFS?**  \nA: BFS works for equal/unweighted edges. Dijkstra works for non-negative weighted edges.\n\n**Q: When should binary search be considered?**  \nA: When input is sorted or the answer predicate is monotonic: false false false true true.\n\n**Q: Why sliding window?**  \nA: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.\n",
          "body_markdown": "**Q: How do you identify DP?**  \nA: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP.\n\n**Q: BFS vs DFS for shortest path?**  \nA: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path.\n\n**Q: Dijkstra vs BFS?**  \nA: BFS works for equal/unweighted edges. Dijkstra works for non-negative weighted edges.\n\n**Q: When should binary search be considered?**  \nA: When input is sorted or the answer predicate is monotonic: false false false true true.\n\n**Q: Why sliding window?**  \nA: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.\n",
          "rawText": "Q: How do you identify DP? A: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP. Q: BFS vs DFS for shortest path? A: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path. Q: Dijkstra vs BFS? A: BFS works for equal/unweighted edges. Dijkstra works for non negative weighted edges. Q: When should binary search be considered? A: When input is sorted or the answer predicate is monotonic: false false false true true. Q: Why sliding window? A: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.",
          "raw_text": "Q: How do you identify DP? A: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP. Q: BFS vs DFS for shortest path? A: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path. Q: Dijkstra vs BFS? A: BFS works for equal/unweighted edges. Dijkstra works for non negative weighted edges. Q: When should binary search be considered? A: When input is sorted or the answer predicate is monotonic: false false false true true. Q: Why sliding window? A: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.",
          "plainText": "Q: How do you identify DP? A: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP. Q: BFS vs DFS for shortest path? A: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path. Q: Dijkstra vs BFS? A: BFS works for equal/unweighted edges. Dijkstra works for non negative weighted edges. Q: When should binary search be considered? A: When input is sorted or the answer predicate is monotonic: false false false true true. Q: Why sliding window? A: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.",
          "plain_text": "Q: How do you identify DP? A: If brute force repeatedly solves the same subproblems and the answer can be composed from smaller answers, try DP. Q: BFS vs DFS for shortest path? A: BFS gives shortest path in unweighted graphs. DFS does not guarantee shortest path. Q: Dijkstra vs BFS? A: BFS works for equal/unweighted edges. Dijkstra works for non negative weighted edges. Q: When should binary search be considered? A: When input is sorted or the answer predicate is monotonic: false false false true true. Q: Why sliding window? A: For contiguous ranges where moving the left/right boundary can maintain a valid condition efficiently.",
          "kind": "section"
        }
      ],
      "tags": [
        "algorithms",
        "dsa",
        "interview",
        "python",
        "dynamic-programming",
        "graphs"
      ],
      "sourceFiles": [
        "CP Algorithms",
        "KACTL Competitive Programming Library"
      ],
      "source_keys": [
        "cp-algorithms",
        "kactl-competitive-programming-library"
      ],
      "order_index": 860,
      "is_featured": true,
      "metadata": {
        "seed_version": "deep-additions-v1",
        "generated_at": "2026-06-27",
        "intended_endpoint": "/api/v1/admin/topics/bulk-upsert-fast?rebuild_bootstrap=true",
        "source_refs": [
          {
            "label": "CP Algorithms",
            "url": "https://cp-algorithms.com/"
          },
          {
            "label": "KACTL Competitive Programming Library",
            "url": "https://github.com/kth-competitive-programming/kactl"
          }
        ],
        "section_strategy": "flat sections with level + parent_id + stable anchors"
      },
      "extra": {
        "learning_outcomes": [
          "Explain the concept in interviews with production-grade clarity",
          "Recognize common mistakes and debug real issues",
          "Apply the topic with working code/configuration examples",
          "Use diagrams to build a mental model before memorizing syntax"
        ],
        "recommended_rendering": {
          "left_sidebar": "topic tree",
          "right_toc": "sections by anchor",
          "scrollspy_key": "section.anchor"
        }
      }
    }
  ]
}